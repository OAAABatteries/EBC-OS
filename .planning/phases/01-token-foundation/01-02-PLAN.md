---
phase: 01-token-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/constants.js
autonomous: true
requirements:
  - TOKN-06
  - TOKN-07

must_haves:
  truths:
    - "Shadow utilities (sm/md/lg) render visibly different from each other in any theme"
    - "Matrix theme shadows remain flat (sm=none, md/lg=green glow) matching its scanline aesthetic"
    - "Blueprint theme shadows use colored cyan glow, not black opacity"
    - "Semantic status colors (--status-approved, --status-pending, --status-denied, --status-in-transit) resolve to the correct theme primitive in each theme"
    - "Semantic phase colors (--phase-active, --phase-estimating, --phase-pre-construction, --phase-completed, --phase-warranty, --phase-in-progress) resolve correctly in each theme"
  artifacts:
    - path: "src/data/constants.js"
      provides: "Per-theme shadow scale and semantic color aliases in all 8 THEMES entries"
      contains: "--shadow-sm"
  key_links:
    - from: "src/data/constants.js (THEMES vars)"
      to: "document.documentElement.style"
      via: "App.jsx applies theme vars to :root"
      pattern: "--shadow-sm|--status-approved|--phase-active"
    - from: "--status-approved"
      to: "--green"
      via: "CSS var() alias reference"
      pattern: "var\\(--green\\)"
---

<objective>
Add per-theme design tokens to all 8 THEMES entries in constants.js — shadow scale (sm/md/lg) and semantic color aliases (status-* and phase-*). These tokens must vary per theme because shadows have different aesthetics (Matrix=none, Blueprint=glow) and semantic colors reference each theme's own primitive colors.

Purpose: Completes the token vocabulary for shadows and semantic colors. Phase 2 components will consume --shadow-sm/md/lg and --status-*/--phase-* tokens.
Output: Updated src/data/constants.js with 15 new vars in each of the 8 theme objects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-token-foundation/01-CONTEXT.md
@.planning/phases/01-token-foundation/01-RESEARCH.md
@.planning/phases/01-token-foundation/01-UI-SPEC.md

<interfaces>
<!-- Existing THEMES structure — each theme has a `vars` object with CSS custom properties -->
<!-- Example (steel theme, lines 14-33 of constants.js): -->
```javascript
steel: {
  name: "Steel", icon: "...", label: "Steel",
  vars: {
    "--bg":"#06080c", "--bg2":"#0c0f16", ...,
    "--amber":"#e09422", "--green":"#10b981", "--red":"#ef4444", "--blue":"#3b82f6", "--yellow":"#eab308",
    "--text2":"#8494ad", "--text3":"#455068",
    "--shadow":"0 2px 12px rgba(0,0,0,0.3)", "--card-shadow":"0 1px 8px rgba(0,0,0,0.2)",
  }
}
```

All 8 themes: steel, blueprint, daylight, matrix, anime, ebc, midnight, cyberpunk.
Each already defines: --green, --amber, --red, --blue, --yellow, --text2, --text3.
Each already defines: --shadow, --card-shadow (existing 2-level shadow — we add 3-level scale alongside).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadow scale tokens to all 8 THEMES</name>
  <files>src/data/constants.js</files>
  <read_first>
    - src/data/constants.js (full THEMES object — all 8 themes, their existing --shadow and --card-shadow values, and color primitives)
    - .planning/phases/01-token-foundation/01-UI-SPEC.md (§Shadow Scale — per-theme values)
    - .planning/phases/01-token-foundation/01-RESEARCH.md (§Per-Theme Additions, §Matrix Theme Shadow Override, §Blueprint Theme Shadow Override)
  </read_first>
  <action>
Add three new shadow tokens (`--shadow-sm`, `--shadow-md`, `--shadow-lg`) to each theme's `vars` object in `src/data/constants.js`. Place them immediately after the existing `"--card-shadow"` line in each theme.

Keep the existing `"--shadow"` and `"--card-shadow"` properties untouched (they are consumed by existing components — removing them would break current styling).

**Exact values per theme:**

**steel:**
```
"--shadow-sm": "0 1px 4px rgba(0,0,0,0.12)",
"--shadow-md": "0 2px 12px rgba(0,0,0,0.28)",
"--shadow-lg": "0 4px 32px rgba(0,0,0,0.45)",
```

**blueprint:**
```
"--shadow-sm": "0 0 0 1px rgba(0,191,239,0.06)",
"--shadow-md": "0 0 12px rgba(0,191,239,0.08)",
"--shadow-lg": "0 0 24px rgba(0,191,239,0.12)",
```

**daylight:**
```
"--shadow-sm": "0 1px 4px rgba(0,0,0,0.06)",
"--shadow-md": "0 2px 8px rgba(0,0,0,0.12)",
"--shadow-lg": "0 4px 20px rgba(0,0,0,0.18)",
```

**matrix:**
```
"--shadow-sm": "none",
"--shadow-md": "0 0 10px rgba(0,255,65,0.04)",
"--shadow-lg": "0 0 20px rgba(0,255,65,0.08)",
```

**anime:**
```
"--shadow-sm": "0 0 8px rgba(255,45,160,0.06)",
"--shadow-md": "0 0 16px rgba(255,45,160,0.10)",
"--shadow-lg": "0 0 28px rgba(255,45,160,0.16)",
```

**ebc:**
```
"--shadow-sm": "0 1px 4px rgba(0,0,0,0.14)",
"--shadow-md": "0 2px 12px rgba(0,0,0,0.30)",
"--shadow-lg": "0 4px 32px rgba(0,0,0,0.45)",
```

**midnight:**
```
"--shadow-sm": "0 1px 4px rgba(0,0,0,0.20)",
"--shadow-md": "0 2px 10px rgba(0,0,0,0.35)",
"--shadow-lg": "0 4px 24px rgba(0,0,0,0.50)",
```

**cyberpunk:**
```
"--shadow-sm": "0 0 6px rgba(0,240,255,0.05)",
"--shadow-md": "0 0 14px rgba(0,240,255,0.08)",
"--shadow-lg": "0 0 24px rgba(0,240,255,0.14)",
```

Design rationale:
- Dark themes (steel, ebc, midnight) use black-opacity shadows with increasing spread
- Light theme (daylight) uses lighter black-opacity
- Neon themes (matrix, blueprint, anime, cyberpunk) use their accent-colored glow shadows
- Matrix sm=none preserves the flat scanline aesthetic (RESEARCH.md Pitfall 1)
  </action>
  <verify>
    <automated>grep -c "shadow-sm" src/data/constants.js && grep -c "shadow-md" src/data/constants.js && grep -c "shadow-lg" src/data/constants.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "shadow-sm" src/data/constants.js` returns 8 (one per theme)
    - `grep -c "shadow-md" src/data/constants.js` returns 8
    - `grep -c "shadow-lg" src/data/constants.js` returns 8
    - Matrix theme contains `"--shadow-sm": "none"`
    - Blueprint theme contains `"--shadow-sm": "0 0 0 1px rgba(0,191,239,0.06)"`
    - Daylight theme contains `"--shadow-sm": "0 1px 4px rgba(0,0,0,0.06)"`
    - Existing `"--shadow"` and `"--card-shadow"` lines are preserved in all 8 themes
  </acceptance_criteria>
  <done>All 8 THEMES entries have --shadow-sm, --shadow-md, --shadow-lg with theme-appropriate values. Matrix shadows are flat/glow, Blueprint shadows are cyan glow, Daylight shadows are lighter opacity, and dark themes use standard black opacity shadows.</done>
</task>

<task type="auto">
  <name>Task 2: Add semantic status and phase color aliases to all 8 THEMES</name>
  <files>src/data/constants.js</files>
  <read_first>
    - src/data/constants.js (after Task 1 — verify shadow tokens are present, then add semantic colors)
    - .planning/phases/01-token-foundation/01-UI-SPEC.md (§Status Semantic Aliases, §Phase Semantic Aliases)
    - .planning/phases/01-token-foundation/01-RESEARCH.md (§Per-Theme Additions — semantic alias values)
  </read_first>
  <action>
Add 12 semantic color alias tokens to each theme's `vars` object. Place them after the shadow tokens added in Task 1. These aliases use `var()` references to existing theme primitives, so the same line works identically in all 8 themes.

**Exact lines to add (same in ALL 8 themes):**

```javascript
// Status semantic aliases (TOKN-07)
"--status-approved":   "var(--green)",
"--status-pending":    "var(--amber)",
"--status-denied":     "var(--red)",
"--status-in-transit": "var(--blue)",
"--status-project":    "var(--text2)",
"--status-office":     "var(--text3)",
// Phase semantic aliases (TOKN-07) — named, not numbered
"--phase-active":           "var(--green)",
"--phase-estimating":       "var(--amber)",
"--phase-pre-construction": "var(--blue)",
"--phase-completed":        "var(--text3)",
"--phase-warranty":         "var(--yellow)",
"--phase-in-progress":      "var(--green)",
```

These 12 lines are IDENTICAL across all 8 themes because they reference `var(--green)`, `var(--amber)`, etc., which are already defined per-theme with different hex values. The aliasing happens at CSS cascade resolution time.

Do NOT modify:
- MoreTabs.jsx PHASE_COLORS array (chart palette, array-indexed — per RESEARCH.md Pitfall 6)
- Any STATUS_COLORS in submittalsPackagePdf.js (jsPDF needs numeric RGB — per RESEARCH.md Pitfall 5)
- Any other file — only constants.js
  </action>
  <verify>
    <automated>grep -c "status-approved" src/data/constants.js && grep -c "phase-active" src/data/constants.js && grep -c "phase-in-progress" src/data/constants.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "status-approved" src/data/constants.js` returns 8 (one per theme)
    - `grep -c "status-pending" src/data/constants.js` returns 8
    - `grep -c "status-denied" src/data/constants.js` returns 8
    - `grep -c "status-in-transit" src/data/constants.js` returns 8
    - `grep -c "status-project" src/data/constants.js` returns 8
    - `grep -c "status-office" src/data/constants.js` returns 8
    - `grep -c "phase-active" src/data/constants.js` returns 8
    - `grep -c "phase-estimating" src/data/constants.js` returns 8
    - `grep -c "phase-pre-construction" src/data/constants.js` returns 8
    - `grep -c "phase-completed" src/data/constants.js` returns 8
    - `grep -c "phase-warranty" src/data/constants.js` returns 8
    - `grep -c "phase-in-progress" src/data/constants.js` returns 8
    - All status/phase values use `var(--green)`, `var(--amber)`, `var(--red)`, `var(--blue)`, `var(--yellow)`, `var(--text2)`, or `var(--text3)` — no hard-coded hex values
    - MoreTabs.jsx is NOT modified
    - submittalsPackagePdf.js is NOT modified
  </acceptance_criteria>
  <done>All 8 THEMES entries have 6 status semantic aliases and 6 phase semantic aliases, all referencing theme primitives via var(). No hex values in semantic tokens. No files outside constants.js modified.</done>
</task>

</tasks>

<verification>
1. Run `grep -c "shadow-sm" src/data/constants.js` — should return 8
2. Run `grep -c "status-approved" src/data/constants.js` — should return 8
3. Run `grep -c "phase-active" src/data/constants.js` — should return 8
4. Verify Matrix theme: `grep -A1 "matrix" src/data/constants.js | head -30` — shadow-sm should be "none"
5. Run the dev server, switch to each theme in the app, open DevTools:
   - `getComputedStyle(document.documentElement).getPropertyValue('--shadow-md')` should return a non-empty value
   - `getComputedStyle(document.documentElement).getPropertyValue('--status-approved')` should resolve to the theme's green color
   - `getComputedStyle(document.documentElement).getPropertyValue('--phase-active')` should resolve to the theme's green color
</verification>

<success_criteria>
- All 8 themes have --shadow-sm, --shadow-md, --shadow-lg with theme-appropriate values
- All 8 themes have 6 status aliases and 6 phase aliases referencing theme primitives
- Existing --shadow and --card-shadow values are preserved (no regression)
- Theme switching still works (no JS errors, no missing vars)
- Matrix theme shadow-sm is "none", Blueprint shadow-sm is cyan glow
</success_criteria>

<output>
After completion, create `.planning/phases/01-token-foundation/01-02-SUMMARY.md`
</output>
