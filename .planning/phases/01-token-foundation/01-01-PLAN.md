---
phase: 01-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tokens.css
  - src/main.jsx
autonomous: true
requirements:
  - TOKN-01
  - TOKN-02
  - TOKN-03
  - TOKN-04
  - TOKN-05

must_haves:
  truths:
    - "A developer can use --space-1 through --space-12 in any CSS rule and get the correct pixel value"
    - "A developer can use --text-sm, --text-base, --text-lg, --text-display for typography sizing"
    - "The --touch-min token resolves to 44px for WCAG 2.5.5 touch target enforcement"
    - "The --focus-ring token produces a visible ring using the theme's amber color"
    - "Transition timing tokens --transition-micro and --transition-state resolve to 150ms and 300ms with standard easing"
  artifacts:
    - path: "src/tokens.css"
      provides: "Universal design tokens (spacing, typography, touch, focus, transitions)"
      contains: "--space-4"
    - path: "src/main.jsx"
      provides: "tokens.css import before app renders"
      contains: "import './tokens.css'"
  key_links:
    - from: "src/main.jsx"
      to: "src/tokens.css"
      via: "CSS import at top of file"
      pattern: "import.*tokens\\.css"
    - from: "src/tokens.css"
      to: "THEMES in constants.js"
      via: "--focus-ring references var(--amber) which is theme-defined"
      pattern: "--focus-ring.*var\\(--amber\\)"
---

<objective>
Create the universal design token file (src/tokens.css) containing all theme-independent CSS custom properties — spacing scale, typography scale, touch target minimum, focus ring, and transition timing — and wire it into the app via main.jsx import.

Purpose: Establishes the shared vocabulary that all subsequent phases consume. These tokens are defined once in :root and never vary across themes.
Output: src/tokens.css with all universal tokens, src/main.jsx with the import line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-token-foundation/01-CONTEXT.md
@.planning/phases/01-token-foundation/01-RESEARCH.md
@.planning/phases/01-token-foundation/01-UI-SPEC.md

<interfaces>
<!-- Key existing code the executor needs to understand -->

From src/main.jsx (lines 1-5):
```javascript
import { StrictMode, Component, useState, useEffect, lazy, Suspense } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.jsx'
```
tokens.css must be imported AFTER index.css but BEFORE App.jsx so that token vars are available when styles.js injects its <style> tag.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/tokens.css with universal design tokens</name>
  <files>src/tokens.css</files>
  <read_first>
    - src/styles.js (current hard-coded spacing, font sizes, transitions to understand what tokens replace)
    - .planning/phases/01-token-foundation/01-UI-SPEC.md (canonical token values)
    - .planning/phases/01-token-foundation/01-RESEARCH.md (complete tokens.css example in §Code Examples)
  </read_first>
  <action>
Create a new file `src/tokens.css` with a single `:root` block containing these exact tokens:

**Spacing scale (TOKN-01) — 9 steps:**
```css
--space-1:   4px;
--space-2:   8px;
--space-3:  12px;
--space-4:  16px;
--space-5:  20px;
--space-6:  24px;
--space-8:  32px;
--space-10: 40px;
--space-12: 48px;
```

**Typography scale (TOKN-02) — 4 primary tokens per UI-SPEC consolidation:**
```css
--text-sm:      11px;
--text-base:    13px;
--text-lg:      18px;
--text-display: 28px;
```

**Font weights — 2 only per UI-SPEC:**
```css
--weight-normal: 400;
--weight-bold:   700;
```

**Line heights:**
```css
--leading-tight:   1.2;
--leading-normal:  1.5;
--leading-relaxed: 1.75;
```

**Letter spacing:**
```css
--tracking-tight:  -0.01em;
--tracking-normal:  0;
--tracking-wide:    0.05em;
--tracking-wider:   0.08em;
```

**Touch target (TOKN-03):**
```css
--touch-min: 44px;
```

**Focus ring (TOKN-04) — references --amber which is theme-defined, resolves lazily at paint time:**
```css
--focus-ring: 0 0 0 2px var(--amber);
```

**Transitions (TOKN-05):**
```css
--duration-micro:  150ms;
--duration-state:  300ms;
--ease-standard:   cubic-bezier(.4, 0, .2, 1);
--transition-micro: var(--duration-micro) var(--ease-standard);
--transition-state: var(--duration-state) var(--ease-standard);
```

Use section comment headers matching the RESEARCH.md style: `/* -- SPACING SCALE -- */`, etc.

Do NOT include:
- Font-family tokens (deferred — Fira vs Barlow unresolved)
- Shadow tokens (those go in THEMES per-theme in Plan 02)
- Semantic color tokens (those go in THEMES per-theme in Plan 02)
- Glass/blur tokens (out of scope per D-08)
  </action>
  <verify>
    <automated>grep -c "\\-\\-space-4" src/tokens.css && grep -c "\\-\\-text-base" src/tokens.css && grep -c "\\-\\-touch-min" src/tokens.css && grep -c "\\-\\-focus-ring" src/tokens.css && grep -c "\\-\\-transition-micro" src/tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - src/tokens.css exists and contains a single :root block
    - src/tokens.css contains `--space-1:   4px` through `--space-12: 48px` (9 spacing tokens)
    - src/tokens.css contains `--text-sm:      11px`, `--text-base:    13px`, `--text-lg:      18px`, `--text-display: 28px` (4 typography tokens)
    - src/tokens.css contains `--weight-normal: 400` and `--weight-bold:   700` (exactly 2 weights)
    - src/tokens.css contains `--leading-tight:   1.2`, `--leading-normal:  1.5`, `--leading-relaxed: 1.75`
    - src/tokens.css contains `--tracking-tight:  -0.01em`, `--tracking-normal:  0`, `--tracking-wide:    0.05em`, `--tracking-wider:   0.08em`
    - src/tokens.css contains `--touch-min: 44px`
    - src/tokens.css contains `--focus-ring: 0 0 0 2px var(--amber)`
    - src/tokens.css contains `--duration-micro:  150ms` and `--duration-state:  300ms`
    - src/tokens.css contains `--ease-standard:   cubic-bezier(.4, 0, .2, 1)`
    - src/tokens.css contains `--transition-micro: var(--duration-micro) var(--ease-standard)`
    - src/tokens.css does NOT contain `--font-family` or `font-family`
    - src/tokens.css does NOT contain `--shadow`
    - src/tokens.css does NOT contain `--status-` or `--phase-`
  </acceptance_criteria>
  <done>src/tokens.css exists with all 9 spacing tokens, 4 typography tokens, 2 weight tokens, 3 line-height tokens, 4 letter-spacing tokens, 1 touch token, 1 focus ring token, and 5 transition tokens — all in a single :root block with no font-family, shadow, or semantic color tokens</done>
</task>

<task type="auto">
  <name>Task 2: Import tokens.css in main.jsx</name>
  <files>src/main.jsx</files>
  <read_first>
    - src/main.jsx (current import order — tokens.css must go after index.css, before App.jsx)
  </read_first>
  <action>
Add one line to `src/main.jsx` at line 4 (after `import './index.css'` and before `import App from './App.jsx'`):

```javascript
import './tokens.css'
```

The final import block should read:
```javascript
import { StrictMode, Component, useState, useEffect, lazy, Suspense } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import './tokens.css'
import App from './App.jsx'
```

This ordering ensures:
1. index.css loads first (reset, PWA safe-area)
2. tokens.css loads second (defines :root custom properties)
3. App.jsx loads third (its styles.js injection consumes token vars via var())
  </action>
  <verify>
    <automated>grep -n "import.*tokens.css" src/main.jsx</automated>
  </verify>
  <acceptance_criteria>
    - src/main.jsx contains the line `import './tokens.css'`
    - The tokens.css import appears AFTER `import './index.css'` and BEFORE `import App from './App.jsx'`
    - No other lines in main.jsx are modified
  </acceptance_criteria>
  <done>main.jsx imports tokens.css in the correct position — after index.css, before App.jsx</done>
</task>

</tasks>

<verification>
1. Run `grep -c "\\-\\-space" src/tokens.css` — should return 9
2. Run `grep -c "\\-\\-text-" src/tokens.css` — should return 4
3. Run `grep "import.*tokens" src/main.jsx` — should show the import line
4. Run the dev server (`npm run dev`) and open browser DevTools console:
   - `getComputedStyle(document.documentElement).getPropertyValue('--space-4')` should return `16px` (or ` 16px`)
   - `getComputedStyle(document.documentElement).getPropertyValue('--touch-min')` should return `44px`
   - `getComputedStyle(document.documentElement).getPropertyValue('--text-base')` should return `13px`
</verification>

<success_criteria>
- src/tokens.css exists with all universal tokens defined in :root
- src/main.jsx imports tokens.css in the correct position
- The app loads without errors (no broken imports, no CSS parse errors)
- All token vars resolve to their expected values in browser DevTools
</success_criteria>

<output>
After completion, create `.planning/phases/01-token-foundation/01-01-SUMMARY.md`
</output>
