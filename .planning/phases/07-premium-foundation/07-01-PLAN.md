---
phase: 07-premium-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
  - src/styles.js
  - src/components/field/PortalHeader.jsx
  - src/components/field/EmptyState.jsx
  - src/components/field/PortalTabBar.jsx
  - src/components/field/FieldButton.jsx
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-05]

must_haves:
  truths:
    - "New users (no localStorage theme) see EBC Brand theme on load"
    - "Eagle logo is visible in PortalHeader on all 8 themes including daylight"
    - "EmptyState icon renders at 40px (not 48px)"
    - "EmptyState heading uses --text-base bold, body uses --text-sm --text2"
    - "PortalTabBar active tab uses --accent color with scale(0.95)"
    - "FieldButton primary variant uses --accent background"
  artifacts:
    - path: "src/App.jsx"
      provides: "Default theme changed to ebc"
      contains: "useLocalStorage(\"theme\", \"ebc\")"
    - path: "src/components/field/PortalHeader.jsx"
      provides: "Theme-aware eagle logo with CSS filter for daylight"
      contains: "portal-header-logo"
    - path: "src/components/field/EmptyState.jsx"
      provides: "Refreshed empty state with 40px icon"
      contains: "size={40}"
    - path: "src/styles.js"
      provides: "Updated CSS for header, tab bar, empty state, button"
      contains: "portal-header-logo--dark"
  key_links:
    - from: "src/App.jsx"
      to: "localStorage theme default"
      via: "useLocalStorage second argument"
      pattern: "useLocalStorage.*theme.*ebc"
    - from: "src/components/field/PortalHeader.jsx"
      to: "src/styles.js"
      via: "CSS class for dark logo variant"
      pattern: "portal-header-logo--dark"
---

<objective>
Apply Premium Construction visual language to existing shared components and set EBC Brand as the default theme.

Purpose: Establish the visual foundation that all Phase 8-10 portal work builds on. Existing users keep their theme; new users see the EBC Brand.
Output: Updated PortalHeader (eagle logo all themes), EmptyState (action-required redesign), PortalTabBar (premium active states), FieldButton (accent primary), and default theme set to "ebc".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-premium-foundation/07-CONTEXT.md
@.planning/phases/07-premium-foundation/07-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/field/PortalHeader.jsx:
```jsx
export function PortalHeader({
  variant = 'employee',  // 'foreman' | 'employee' | 'driver'
  title,
  userName,
  logoutAction,
  languageToggle,
  settingsAction,
  projectSelector,
  className,
  t,
  network,
})
```

From src/components/field/EmptyState.jsx:
```jsx
export function EmptyState({ icon: Icon = Inbox, heading, message, action, className, t })
```

From src/App.jsx line 354:
```jsx
const [theme, setTheme] = useLocalStorage("theme", "daylight");
```

From src/styles.js existing CSS classes:
```css
.header{display:flex;align-items:center;gap:16px;padding:0 24px;height:54px;...}
.portal-header-logo{height:28px;width:auto;object-fit:contain}
.empty-state{display:flex;flex-direction:column;...}
.empty-state-icon{color:var(--text3)}
.empty-state-heading{font-size:var(--text-lg);font-weight:var(--weight-bold);...}
.empty-state-body{font-size:var(--text-base);...color:var(--text2);...}
.field-tab-item.active{color:var(--amber);font-weight:var(--weight-bold)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Default theme + PortalHeader eagle logo + header visual refresh</name>
  <files>src/App.jsx, src/components/field/PortalHeader.jsx, src/styles.js</files>
  <read_first>
    - src/App.jsx (line 354 — the useLocalStorage theme default)
    - src/components/field/PortalHeader.jsx (current implementation)
    - src/styles.js (lines 12-19 — .header and .portal-header-logo classes)
    - src/data/constants.js (theme names to identify which themes have light backgrounds)
  </read_first>
  <action>
1. In `src/App.jsx` line 354, change `useLocalStorage("theme", "daylight")` to `useLocalStorage("theme", "ebc")`. This makes EBC Brand the default for new users per VIS-01. Note: CONTEXT.md D-08 references `constants.js`, but RESEARCH.md Critical Discovery confirms the actual default is in `src/App.jsx` line 354 via the `useLocalStorage` second argument. This plan implements the research-confirmed location. Existing users with a saved theme keep their selection.

2. In `src/components/field/PortalHeader.jsx`:
   - Add `theme` prop (optional, default `null`) to the function signature. Note: the theme prop defaults to null, so the daylight CSS filter is dormant until portal consumers pass the prop in Phases 8-9. This is intentional and within phase scope — the component is ready, consumers will be wired in downstream phases.
   - On the eagle `<img>` element, add conditional className: `className={\`portal-header-logo\${theme === 'daylight' ? ' portal-header-logo--dark' : ''}\`}`. This applies a CSS filter to invert the white eagle to dark for the light-background daylight theme per D-05.
   - Keep the existing `onError` handler per D-07.
   - Add `alt="EBC Eagle"` (change from current `alt="EBC"`) for accessibility.
   - Update header bottom border: add `border-bottom: 1px solid rgba(255,127,33,0.15)` via a new CSS class `.portal-header-accent-border` applied to the `<header>` element. This is the accent-tinted border from the UI-SPEC.

3. In `src/styles.js`:
   - Add after the existing `.portal-header-logo` rule:
     `.portal-header-logo--dark{filter:invert(1) brightness(0.3)}`
   - Add: `.portal-header-accent-border{border-bottom-color:rgba(255,127,33,0.15)}`
   - Note: The border-bottom on `.header` already exists as `1px solid var(--glass-border)`. The `.portal-header-accent-border` class overrides only the color, not the width/style. This uses the EBC amber at 0.15 opacity. For non-EBC themes this is acceptable since --glass-border is the fallback and the accent tint is subtle.

Do NOT change PortalHeader height (stays 54px). Do NOT add backdrop-filter (already present on .header).
  </action>
  <verify>
    <automated>grep -n "useLocalStorage.*theme.*ebc" src/App.jsx && grep -n "portal-header-logo--dark" src/styles.js && grep -n "theme" src/components/field/PortalHeader.jsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/App.jsx contains `useLocalStorage("theme", "ebc")`
    - src/components/field/PortalHeader.jsx signature includes `theme` parameter
    - src/components/field/PortalHeader.jsx contains `portal-header-logo--dark`
    - src/components/field/PortalHeader.jsx contains `alt="EBC Eagle"`
    - src/styles.js contains `.portal-header-logo--dark{filter:invert(1) brightness(0.3)}`
    - src/styles.js contains `.portal-header-accent-border`
  </acceptance_criteria>
  <done>Default theme is "ebc", eagle logo is theme-aware with CSS filter for daylight theme, header has accent-tinted bottom border</done>
</task>

<task type="auto">
  <name>Task 2: EmptyState redesign + PortalTabBar + FieldButton visual refresh</name>
  <files>src/components/field/EmptyState.jsx, src/components/field/PortalTabBar.jsx, src/components/field/FieldButton.jsx, src/styles.js</files>
  <read_first>
    - src/components/field/EmptyState.jsx (current implementation)
    - src/components/field/PortalTabBar.jsx (current implementation)
    - src/components/field/FieldButton.jsx (current implementation)
    - src/styles.js (lines 288-328 — FIELD COMPONENTS section with .empty-state, .field-tab-item, .btn classes)
  </read_first>
  <action>
1. In `src/components/field/EmptyState.jsx`:
   - Change `size={48}` to `size={40}` on the Icon render (per D-10, UI-SPEC icon at 40px).
   - No other JSX changes needed. The `action` prop is already a ReactNode slot. Enforcement that action is always populated (VIS-05/D-09) is a caller convention — EmptyState itself renders `{action && ...}` which is correct.

2. In `src/styles.js` — update EmptyState CSS classes:
   - Change `.empty-state-heading` from `font-size:var(--text-lg)` to `font-size:var(--text-base);font-weight:var(--weight-bold)` (per UI-SPEC: heading is --text-base bold).
   - Confirm `.empty-state-body` already has `font-size:var(--text-base)` — change to `font-size:var(--text-sm)` and confirm `color:var(--text2)` (per UI-SPEC: body is --text-sm --text2).
   - Confirm `.empty-state-icon` already has `color:var(--text3)` — no change needed.

3. In `src/styles.js` — update PortalTabBar active state:
   - Change `.field-tab-item.active` from `{color:var(--amber);font-weight:var(--weight-bold)}` to `{color:var(--accent);font-weight:var(--weight-bold);transform:scale(0.95)}` (per UI-SPEC: active tab uses --accent + scale(0.95)).
   - Add `.field-tab-bar` update: ensure `border-top:1px solid rgba(255,255,255,0.05)` is present (per UI-SPEC). The existing border-top uses `var(--glass-border)` which is `rgba(255,255,255,0.06)` — close enough, no change needed.

4. In `src/styles.js` — no FieldButton CSS changes needed. The existing `.btn-primary` class already uses `background:var(--amber)` which is the same as `var(--accent)` (Phase 06 aliased --accent to var(--amber) across all themes). FieldButton.jsx needs no code changes.

5. In `src/components/field/PortalTabBar.jsx` — no JSX changes needed. The CSS class update in styles.js handles the visual refresh.

6. In `src/components/field/FieldButton.jsx` — no changes needed. Already uses correct token classes.

7. **VIS-03 audit for FieldButton and FieldCard:** Read both files and confirm they already meet the Premium Construction visual language:
   - FieldButton: `.btn-primary` uses `background:var(--amber)` (which is `var(--accent)` per Phase 06 alias). No hex literals. Already compliant.
   - FieldCard: `.field-card` uses `background:var(--bg2)`, `border-radius:var(--radius)`, `border:1px solid var(--glass-border)`. All token-based. Already compliant. FieldCard stays unchanged per D-01 (PremiumCard is new alongside FieldCard).
  </action>
  <verify>
    <automated>grep -n "size={40}" src/components/field/EmptyState.jsx && grep "empty-state-heading" src/styles.js && grep "empty-state-body" src/styles.js && grep "field-tab-item.active" src/styles.js && grep "var(--amber)\|var(--accent)" src/components/field/FieldButton.jsx && grep "var(--bg2)" src/components/field/FieldCard.jsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/field/EmptyState.jsx contains `size={40}` (not size={48})
    - src/styles.js `.empty-state-heading` contains `font-size:var(--text-base)`
    - src/styles.js `.empty-state-body` contains `font-size:var(--text-sm)`
    - src/styles.js `.empty-state-body` contains `color:var(--text2)`
    - src/styles.js `.field-tab-item.active` contains `scale(0.95)`
    - src/styles.js `.field-tab-item.active` contains `var(--accent)` or `var(--amber)`
    - VIS-03 FieldButton audit: .btn-primary uses var(--amber)/var(--accent), no inline styles — confirmed unchanged and compliant
    - VIS-03 FieldCard audit: .field-card uses var(--bg2), var(--radius), var(--glass-border) — confirmed unchanged and compliant per D-01
  </acceptance_criteria>
  <done>EmptyState icon is 40px, heading is --text-base bold, body is --text-sm --text2. PortalTabBar active state uses --accent with scale(0.95). FieldButton unchanged (already correct).</done>
</task>

</tasks>

<verification>
- `grep -n 'useLocalStorage.*theme.*ebc' src/App.jsx` returns a match
- `grep -n 'portal-header-logo--dark' src/styles.js` returns a match
- `grep -n 'size={40}' src/components/field/EmptyState.jsx` returns a match
- `grep 'empty-state-heading.*text-base' src/styles.js` returns a match
- `grep 'field-tab-item.active.*scale' src/styles.js` returns a match
- `npx vitest run --reporter=verbose` passes (no existing tests broken)
</verification>

<success_criteria>
1. Default theme is "ebc" — new users see EBC Brand on first load
2. Eagle logo visible on all themes — daylight theme applies CSS filter for contrast
3. EmptyState uses 40px icon, --text-base bold heading, --text-sm --text2 body
4. PortalTabBar active tab uses --accent color with scale(0.95)
5. No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/07-premium-foundation/07-01-SUMMARY.md`
</output>
