---
phase: 02-shared-field-components
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/field/FieldCard.jsx
  - src/components/field/FieldCard.test.jsx
  - src/components/field/FieldInput.jsx
  - src/components/field/FieldSelect.jsx
  - src/components/field/FieldInput.test.jsx
  - src/components/field/index.js
autonomous: true
requirements:
  - COMP-02
  - COMP-03
must_haves:
  truths:
    - "FieldCard renders in default and glass variants using existing .card/.card-glass classes"
    - "FieldCard hover transform is gated behind @media (hover: hover) to prevent touch layout shift"
    - "FieldInput renders with 44px min-height and passes inputMode prop to underlying input"
    - "FieldInput shows error state with red border and error message below"
    - "FieldSelect renders with 44px min-height and proper focus ring"
  artifacts:
    - path: "src/components/field/FieldCard.jsx"
      provides: "Theme-aware card wrapper with default/glass variants"
      exports: ["FieldCard"]
    - path: "src/components/field/FieldInput.jsx"
      provides: "Touch-target input with error states and inputmode"
      exports: ["FieldInput"]
    - path: "src/components/field/FieldSelect.jsx"
      provides: "Touch-target select with focus ring"
      exports: ["FieldSelect"]
  key_links:
    - from: "src/components/field/FieldCard.jsx"
      to: "src/styles.js"
      via: "className composition with .card/.card-glass and .field-card"
      pattern: "card.*field-card"
    - from: "src/components/field/FieldInput.jsx"
      to: "src/styles.js"
      via: "className composition with .form-input and .field-input"
      pattern: "form-input.*field-input"
---

<objective>
Build FieldCard (COMP-02) and FieldInput/FieldSelect (COMP-03) — the two remaining leaf primitives. These are form and layout building blocks consumed by MaterialRequestCard and portal forms.

Purpose: FieldCard is the base wrapper for MaterialRequestCard and every card-based UI. FieldInput/FieldSelect are the standard form controls for all portal forms with proper inputmode for mobile keyboards.
Output: 3 components with tests, exported from barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-field-components/02-CONTEXT.md
@.planning/phases/02-shared-field-components/02-UI-SPEC.md
@src/styles.js
@src/tokens.css

<interfaces>
<!-- Existing CSS classes these components extend (from src/styles.js) -->

.card — background:var(--bg2); border:1px solid var(--border); border-radius:12px; padding:20px; box-shadow:0 2px 8px rgba(0,0,0,0.08)
.card:hover — border-color:var(--border2); box-shadow:0 4px 16px rgba(0,0,0,0.12); transform:translateY(-1px)
.card-glass — backdrop-filter:blur(20px) saturate(1.6); background:var(--glass-bg); border:1px solid var(--glass-border)

.form-input, .form-select — background:var(--bg); border:1px solid var(--border); border-radius:var(--radius-sm); padding:8px 12px; font-size:var(--text-base); color:var(--text); font-family:var(--font-body)
.form-input:focus, .form-select:focus — border-color:var(--amber); box-shadow:0 0 0 2px var(--amber-dim)
.form-label — font-size:10px; text-transform:uppercase; letter-spacing:0.6px; color:var(--text3); font-weight:600

<!-- New CSS classes from Plan 01 (in styles.js FIELD COMPONENTS section) -->
.field-card — padding:var(--space-4)
@media (hover: hover) { .field-card:hover { transform:translateY(-1px) } }
@media (hover: none) { .field-card:hover { transform:none } }

.field-input, .field-select — min-height:var(--touch-min); padding:var(--space-3) var(--space-4)
.field-input-error — border-color:var(--red); box-shadow:0 0 0 2px var(--red-dim)
.field-input-error-msg — font-size:var(--text-sm); color:var(--red); margin-top:var(--space-1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build FieldCard component (COMP-02)</name>
  <files>src/components/field/FieldCard.jsx, src/components/field/FieldCard.test.jsx</files>
  <read_first>
    - src/styles.js (lines 47-52 — .card and .card-glass definitions, and FIELD COMPONENTS section for .field-card)
    - .planning/phases/02-shared-field-components/02-UI-SPEC.md (FieldCard section)
    - .planning/phases/02-shared-field-components/02-RESEARCH.md (Pitfall 6 — hover on touch)
  </read_first>
  <behavior>
    - Test: FieldCard default variant renders div with className containing "card field-card"
    - Test: FieldCard variant="glass" renders div with className containing "card-glass field-card"
    - Test: FieldCard renders children inside the card div
    - Test: FieldCard passes className prop for additional styling (merged with card classes)
    - Test: FieldCard passes arbitrary props (onClick, data-testid, etc.) to underlying div
  </behavior>
  <action>
    Write test file FIRST (RED), then implement (GREEN).

    **FieldCard.jsx**:
    - Props: `{ variant = "default", children, className, ...props }`
    - Variant class map: `{ default: "card", glass: "card-glass" }`
    - Renders:
      ```jsx
      <div className={`${baseClass} field-card ${className || ''}`.trim()} {...props}>
        {children}
      </div>
      ```
    - No `t` prop needed — FieldCard is a layout wrapper, not a text-rendering component.
    - No inline styles. No hex literals. Hover behavior is fully CSS-controlled via `.field-card` media queries added in Plan 01.

    Export from barrel: add `export { FieldCard } from './FieldCard';` to index.js.
  </action>
  <verify>
    <automated>npx vitest run src/components/field/FieldCard.test.jsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - FieldCard.jsx exports a named function `FieldCard`
    - FieldCard.jsx does NOT contain any hex color literals
    - FieldCard.jsx does NOT contain any `style={{` inline styles
    - FieldCard.jsx renders a `<div>` with className containing both `card` (or `card-glass`) and `field-card`
    - `npx vitest run src/components/field/FieldCard.test.jsx` exits 0
    - src/components/field/index.js contains `export { FieldCard }`
  </acceptance_criteria>
  <done>FieldCard renders in default/glass variants, hover gated for touch via CSS, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build FieldInput and FieldSelect components (COMP-03)</name>
  <files>src/components/field/FieldInput.jsx, src/components/field/FieldSelect.jsx, src/components/field/FieldInput.test.jsx</files>
  <read_first>
    - src/styles.js (lines 86-93 — .form-input, .form-select, .form-label definitions, and FIELD COMPONENTS section for .field-input, .field-input-error)
    - .planning/phases/02-shared-field-components/02-UI-SPEC.md (FieldInput/FieldSelect section)
  </read_first>
  <behavior>
    - Test: FieldInput renders an input with className containing "form-input field-input focus-visible"
    - Test: FieldInput renders a label with className "form-label" when label prop is provided
    - Test: FieldInput passes inputMode prop through to the input element's inputMode attribute
    - Test: FieldInput with error=true adds "field-input-error" class to the input
    - Test: FieldInput with errorMessage renders a div with class "field-input-error-msg" and the error text
    - Test: FieldInput passes placeholder, value, onChange to underlying input
    - Test: FieldInput with disabled=true renders a disabled input with opacity style
    - Test: FieldSelect renders a select element with className containing "form-select field-select focus-visible"
    - Test: FieldSelect renders children (option elements) inside select
    - Test: FieldSelect renders a label when label prop is provided
  </behavior>
  <action>
    Write test file FIRST (RED), then implement (GREEN).

    **FieldInput.jsx**:
    - Props: `{ label, error, errorMessage, className, inputMode, disabled, t, id, ...props }`
    - Generate a stable id if not provided: `const inputId = id || \`field-input-\${label?.replace(/\\s/g, '-').toLowerCase() || 'default'}\``
    - Renders:
      ```jsx
      <div className="field-input-wrapper">
        {label && <label htmlFor={inputId} className="form-label">{t ? t(label) : label}</label>}
        <input
          id={inputId}
          className={`form-input field-input focus-visible ${error ? 'field-input-error' : ''} ${className || ''}`.trim()}
          inputMode={inputMode}
          disabled={disabled}
          style={disabled ? { opacity: 0.45, cursor: 'not-allowed' } : undefined}
          {...props}
        />
        {error && errorMessage && <div className="field-input-error-msg">{t ? t(errorMessage) : errorMessage}</div>}
      </div>
      ```

    **FieldSelect.jsx** (separate file, same test file):
    - Props: `{ label, className, disabled, children, t, id, ...props }`
    - Same label and id pattern as FieldInput.
    - Renders:
      ```jsx
      <div className="field-select-wrapper">
        {label && <label htmlFor={selectId} className="form-label">{t ? t(label) : label}</label>}
        <select
          id={selectId}
          className={`form-select field-select focus-visible ${className || ''}`.trim()}
          disabled={disabled}
          style={disabled ? { opacity: 0.45, cursor: 'not-allowed' } : undefined}
          {...props}
        >
          {children}
        </select>
      </div>
      ```

    Export both from barrel:
    - `export { FieldInput } from './FieldInput';`
    - `export { FieldSelect } from './FieldSelect';`
  </action>
  <verify>
    <automated>npx vitest run src/components/field/FieldInput.test.jsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - FieldInput.jsx exports a named function `FieldInput`
    - FieldSelect.jsx exports a named function `FieldSelect`
    - FieldInput.jsx does NOT contain any hex color literals
    - FieldSelect.jsx does NOT contain any hex color literals
    - FieldInput renders `<input>` with className containing `form-input`, `field-input`, `focus-visible`
    - FieldInput with `inputMode="numeric"` passes `inputMode` attribute to the DOM input element
    - FieldInput with `error=true` adds `field-input-error` class to input
    - FieldInput with `errorMessage` renders error text in a `.field-input-error-msg` div
    - FieldInput renders a `<label>` with class `form-label` when label prop provided
    - FieldSelect renders `<select>` with className containing `form-select`, `field-select`, `focus-visible`
    - `npx vitest run src/components/field/FieldInput.test.jsx` exits 0
    - src/components/field/index.js contains `export { FieldInput }` and `export { FieldSelect }`
  </acceptance_criteria>
  <done>FieldInput/FieldSelect render at 44px min-height, support inputMode, error states, labels, all tests pass</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` all test files pass
- `grep -rn "#[0-9a-fA-F]" src/components/field/FieldCard.jsx src/components/field/FieldInput.jsx src/components/field/FieldSelect.jsx` returns zero results
- `grep "export" src/components/field/index.js` shows FieldCard, FieldInput, FieldSelect added
</verification>

<success_criteria>
FieldCard, FieldInput, and FieldSelect are built, tested, and exported. FieldCard extends .card/.card-glass with touch-safe hover. FieldInput/FieldSelect enforce 44px touch targets and support inputMode/error states. Zero hex literals. All tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-field-components/02-03-SUMMARY.md`
</output>
