---
phase: 02-shared-field-components
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/components/field/EmptyState.jsx
  - src/components/field/EmptyState.test.jsx
  - src/components/field/AsyncState.jsx
  - src/components/field/AsyncState.test.jsx
  - src/components/field/index.js
autonomous: true
requirements:
  - COMP-07
  - COMP-09
must_haves:
  truths:
    - "EmptyState renders centered layout with icon, heading, body copy, and optional action button"
    - "AsyncState renders loading skeleton when loading=true"
    - "AsyncState renders EmptyState when empty=true and not loading"
    - "AsyncState renders error display when error is set (priority over empty)"
    - "AsyncState renders children when all three flags are clear"
  artifacts:
    - path: "src/components/field/EmptyState.jsx"
      provides: "Centered empty state with icon, message, optional action"
      exports: ["EmptyState"]
    - path: "src/components/field/AsyncState.jsx"
      provides: "Four-state wrapper (loading/empty/error/success)"
      exports: ["AsyncState"]
  key_links:
    - from: "src/components/field/AsyncState.jsx"
      to: "src/components/field/EmptyState.jsx"
      via: "renders EmptyState when empty=true"
      pattern: "EmptyState"
    - from: "src/components/field/AsyncState.jsx"
      to: "src/components/field/LoadingSpinner.jsx"
      via: "renders LoadingSpinner when loading=true"
      pattern: "LoadingSpinner"
---

<objective>
Build EmptyState (COMP-07) and AsyncState (COMP-09) — the composite async state management components. AsyncState wraps any data-fetching UI and delegates to LoadingSpinner/EmptyState/error display so portals never manage that logic manually.

Purpose: AsyncState is the single most reused wrapper in Phases 3-5. Every data list in every portal will use it. EmptyState is also used standalone for static empty states.
Output: 2 components with tests, exported from barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-field-components/02-CONTEXT.md
@.planning/phases/02-shared-field-components/02-UI-SPEC.md
@.planning/phases/02-shared-field-components/02-RESEARCH.md

@.planning/phases/02-shared-field-components/02-02-SUMMARY.md

<interfaces>
<!-- Components from Plan 02 that this plan consumes -->

From src/components/field/LoadingSpinner.jsx:
  export function LoadingSpinner({ size = 20, className, t })
  export function Skeleton({ width, height, className, style })

From src/components/field/FieldButton.jsx:
  export function FieldButton({ variant = "primary", loading, disabled, children, className, t, ...props })

<!-- CSS classes from Plan 01 (in styles.js FIELD COMPONENTS section) -->
.empty-state — display:flex; flex-direction:column; align-items:center; justify-content:center; padding:var(--space-6) var(--space-4); text-align:center; gap:var(--space-3)
.empty-state-icon — color:var(--text3)
.empty-state-heading — font-size:var(--text-lg); font-weight:var(--weight-bold); color:var(--text)
.empty-state-body — font-size:var(--text-base); color:var(--text2); line-height:var(--leading-normal)

.async-error — display:flex; flex-direction:column; align-items:center; justify-content:center; padding:var(--space-6) var(--space-4); text-align:center; gap:var(--space-3)
.async-error-heading — font-size:var(--text-lg); font-weight:var(--weight-bold); color:var(--text)
.async-error-body — font-size:var(--text-base); color:var(--text2); line-height:var(--leading-normal)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build EmptyState component (COMP-07)</name>
  <files>src/components/field/EmptyState.jsx, src/components/field/EmptyState.test.jsx</files>
  <read_first>
    - src/styles.js (FIELD COMPONENTS section — .empty-state, .empty-state-icon, .empty-state-heading, .empty-state-body)
    - .planning/phases/02-shared-field-components/02-UI-SPEC.md (EmptyState section + Copywriting Contract)
    - src/components/field/FieldButton.jsx (interface for action button rendering)
  </read_first>
  <behavior>
    - Test: EmptyState renders a div with className "empty-state"
    - Test: EmptyState renders the provided icon as a Lucide component at 48px size with className "empty-state-icon"
    - Test: EmptyState renders heading text in a div with className "empty-state-heading"
    - Test: EmptyState renders body/message text in a div with className "empty-state-body"
    - Test: EmptyState renders action prop (ReactNode) when provided
    - Test: EmptyState does NOT render action area when action prop is not provided
    - Test: EmptyState with no message uses t("Nothing here yet") as default heading
    - Test: EmptyState translates message through t() when t prop is provided
  </behavior>
  <action>
    Write test file FIRST (RED), then implement (GREEN).

    **EmptyState.jsx**:
    - Props: `{ icon: Icon, heading, message, action, className, t }`
    - `Icon` is a Lucide React component passed as a prop (e.g., `icon={Package}`). Default: `import { Inbox } from 'lucide-react'` as fallback.
    - Default heading: `t ? t("Nothing here yet") : "Nothing here yet"`
    - Default message: `t ? t("Check back later or contact your foreman.") : "Check back later or contact your foreman."`
    - Renders:
      ```jsx
      <div className={`empty-state ${className || ''}`.trim()}>
        {Icon && <Icon size={48} className="empty-state-icon" aria-hidden="true" />}
        <div className="empty-state-heading">{heading || defaultHeading}</div>
        {(message || defaultMessage) && <div className="empty-state-body">{message || defaultMessage}</div>}
        {action && <div className="empty-state-action">{action}</div>}
      </div>
      ```
    - No inline styles. No hex literals. The action prop is typically a `<FieldButton variant="ghost">` but EmptyState doesn't import FieldButton — it renders whatever ReactNode the caller passes.

    Export from barrel: add `export { EmptyState } from './EmptyState';` to index.js.
  </action>
  <verify>
    <automated>npx vitest run src/components/field/EmptyState.test.jsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - EmptyState.jsx exports a named function `EmptyState`
    - EmptyState.jsx does NOT contain any hex color literals
    - EmptyState.jsx renders a `<div>` with className `empty-state`
    - EmptyState.jsx renders `.empty-state-icon`, `.empty-state-heading`, `.empty-state-body` children
    - EmptyState.jsx uses t() for default heading and message text
    - `npx vitest run src/components/field/EmptyState.test.jsx` exits 0
    - src/components/field/index.js contains `export { EmptyState }`
  </acceptance_criteria>
  <done>EmptyState renders centered icon + heading + body + optional action, theme-aware, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build AsyncState wrapper component (COMP-09)</name>
  <files>src/components/field/AsyncState.jsx, src/components/field/AsyncState.test.jsx</files>
  <read_first>
    - src/components/field/EmptyState.jsx (just created — props interface)
    - src/components/field/LoadingSpinner.jsx (LoadingSpinner and Skeleton exports)
    - .planning/phases/02-shared-field-components/02-UI-SPEC.md (AsyncState section + Copywriting Contract)
    - .planning/phases/02-shared-field-components/02-RESEARCH.md (Pattern 3: Children Wrapper)
  </read_first>
  <behavior>
    - Test: AsyncState with loading=true renders LoadingSpinner (finds svg with class animate-spin)
    - Test: AsyncState with loading=true and skeleton prop renders the skeleton ReactNode instead of LoadingSpinner
    - Test: AsyncState with loading=true ignores empty and error props (loading takes priority)
    - Test: AsyncState with error="Network error" renders error display with heading "Something went wrong." and the error message
    - Test: AsyncState with error set and empty=true renders error (error takes priority over empty)
    - Test: AsyncState with empty=true renders EmptyState with emptyMessage
    - Test: AsyncState with empty=true and emptyAction renders EmptyState with the action prop
    - Test: AsyncState with loading=false, empty=false, error=null renders children
    - Test: AsyncState error display uses t() for "Something went wrong." heading
  </behavior>
  <action>
    Write test file FIRST (RED), then implement (GREEN).

    **AsyncState.jsx**:
    - Props: `{ loading, empty, error, emptyMessage, emptyIcon, emptyAction, skeleton, children, t }`
    - Import: `LoadingSpinner` from `./LoadingSpinner`, `EmptyState` from `./EmptyState`
    - Import: `AlertTriangle` from `lucide-react` for error icon
    - Priority: `loading` > `error` > `empty` > `children` (per UI-SPEC contract)
    - Renders:
      ```jsx
      if (loading) return skeleton || <div style={{ display: 'flex', justifyContent: 'center', padding: 'var(--space-6)' }}><LoadingSpinner t={t} /></div>;
      if (error) return (
        <div className="async-error">
          <AlertTriangle size={48} className="empty-state-icon" aria-hidden="true" />
          <div className="async-error-heading">{t ? t("Something went wrong.") : "Something went wrong."}</div>
          <div className="async-error-body">{typeof error === 'string' ? error : (t ? t("Pull down to refresh, or contact support if this keeps happening.") : "Pull down to refresh, or contact support if this keeps happening.")}</div>
        </div>
      );
      if (empty) return <EmptyState icon={emptyIcon} message={emptyMessage} action={emptyAction} t={t} />;
      return children;
      ```
    - No hex literals. The loading center wrapper uses a CSS var for padding.

    Export from barrel: add `export { AsyncState } from './AsyncState';` to index.js.
  </action>
  <verify>
    <automated>npx vitest run src/components/field/AsyncState.test.jsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - AsyncState.jsx exports a named function `AsyncState`
    - AsyncState.jsx imports `LoadingSpinner` from `./LoadingSpinner`
    - AsyncState.jsx imports `EmptyState` from `./EmptyState`
    - AsyncState.jsx does NOT contain any hex color literals
    - AsyncState.jsx renders LoadingSpinner when loading=true
    - AsyncState.jsx renders custom skeleton when loading=true and skeleton prop provided
    - AsyncState.jsx renders `.async-error` div with `.async-error-heading` when error is set
    - AsyncState.jsx renders EmptyState when empty=true
    - AsyncState.jsx renders children when no state flags are active
    - Priority is loading > error > empty > children
    - `npx vitest run src/components/field/AsyncState.test.jsx` exits 0
    - src/components/field/index.js contains `export { AsyncState }`
  </acceptance_criteria>
  <done>AsyncState wraps four mutually exclusive render states, delegates to LoadingSpinner/EmptyState, tests pass</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` all test files pass (cumulative: StatusBadge, LoadingSpinner, FieldButton, FieldCard, FieldInput, EmptyState, AsyncState)
- `grep -rn "#[0-9a-fA-F]" src/components/field/EmptyState.jsx src/components/field/AsyncState.jsx` returns zero results
- `grep "export" src/components/field/index.js` shows EmptyState and AsyncState added
</verification>

<success_criteria>
EmptyState renders centered empty state with icon/heading/body/action. AsyncState wraps any async operation and correctly delegates to loading, error, empty, or success renders. Both consume existing CSS classes and pass t() for bilingual support. All tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-field-components/02-04-SUMMARY.md`
</output>
