---
phase: 02-shared-field-components
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/field/StatusBadge.jsx
  - src/components/field/StatusBadge.test.jsx
  - src/components/field/LoadingSpinner.jsx
  - src/components/field/LoadingSpinner.test.jsx
  - src/components/field/FieldButton.jsx
  - src/components/field/FieldButton.test.jsx
  - src/components/field/index.js
autonomous: true
requirements:
  - COMP-01
  - COMP-06
  - COMP-08
must_haves:
  truths:
    - "StatusBadge maps all 6 status values to correct badge-* CSS classes"
    - "LoadingSpinner renders a spinning Loader2 icon in amber color"
    - "FieldButton renders at 44px minimum height in primary, ghost, and danger variants"
    - "FieldButton shows a loading spinner when loading prop is true and hides children"
  artifacts:
    - path: "src/components/field/StatusBadge.jsx"
      provides: "Semantic status-to-badge-class mapper"
      exports: ["StatusBadge"]
    - path: "src/components/field/LoadingSpinner.jsx"
      provides: "Lucide Loader2 spinner and skeleton block"
      exports: ["LoadingSpinner", "Skeleton"]
    - path: "src/components/field/FieldButton.jsx"
      provides: "Touch-target button with loading state and three variants"
      exports: ["FieldButton"]
  key_links:
    - from: "src/components/field/FieldButton.jsx"
      to: "src/components/field/LoadingSpinner.jsx"
      via: "imports Loader2 for loading state"
      pattern: "Loader2"
    - from: "src/components/field/StatusBadge.jsx"
      to: "src/styles.js"
      via: "className composition with badge-* classes"
      pattern: "badge-green|badge-red|badge-amber|badge-blue|badge-muted"
---

<objective>
Build the three leaf-level components that have zero internal dependencies: StatusBadge (COMP-06), LoadingSpinner/Skeleton (COMP-08), and FieldButton (COMP-01). These are consumed by almost every other component in the phase.

Purpose: Establish the atomic building blocks. StatusBadge is used by MaterialRequestCard. LoadingSpinner/Skeleton is used by AsyncState and FieldButton. FieldButton is used by EmptyState, FieldSignaturePad, and MaterialRequestCard.
Output: 3 working components with passing tests, exported from barrel index.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-field-components/02-CONTEXT.md
@.planning/phases/02-shared-field-components/02-RESEARCH.md
@.planning/phases/02-shared-field-components/02-UI-SPEC.md
@src/styles.js
@src/tokens.css

<interfaces>
<!-- Existing CSS classes these components extend (from src/styles.js) -->

.btn — display:inline-flex; align-items:center; gap:6px; padding:8px 16px; min-height:36px; border-radius:var(--radius-sm); font-size:var(--text-base); font-weight:var(--weight-bold); cursor:pointer; transition:all var(--transition-micro)
.btn-primary — background:var(--amber); color:#000; box-shadow:0 2px 8px var(--amber-glow)
.btn-ghost — background:transparent; border:1px solid var(--border); color:var(--text2)
.btn-danger — background:var(--red-dim); color:var(--red); border:1px solid transparent
.btn:active — transform:scale(0.97)
.touch-target — min-height:var(--touch-min); min-width:var(--touch-min)
.focus-visible:focus-visible — outline:none; box-shadow:var(--focus-ring)

.badge — display:inline-flex; align-items:center; padding:3px 10px; border-radius:99px; font-size:var(--text-sm); font-weight:var(--weight-bold)
.badge-green — background:var(--green-dim); color:var(--green)
.badge-red — background:var(--red-dim); color:var(--red)
.badge-amber — background:var(--amber-dim); color:var(--amber)
.badge-blue — background:var(--blue-dim); color:var(--blue)
.badge-muted — background:var(--bg3); color:var(--text3)

From tokens.css:
--touch-min: 44px
--text-sm: 11px
--text-base: 13px
--focus-ring: 0 0 0 2px var(--amber)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build StatusBadge component (COMP-06)</name>
  <files>src/components/field/StatusBadge.jsx, src/components/field/StatusBadge.test.jsx</files>
  <read_first>
    - src/styles.js (lines 135-141 — badge class definitions)
    - src/tabs/DeliveriesTab.jsx (lines 1-40 — existing STATUS_COLORS pattern to replace)
    - .planning/phases/02-shared-field-components/02-UI-SPEC.md (StatusBadge section)
    - .planning/phases/02-shared-field-components/02-RESEARCH.md (StatusBadge code example)
  </read_first>
  <behavior>
    - Test: StatusBadge with status="approved" renders a span with className containing "badge badge-green"
    - Test: StatusBadge with status="pending" renders a span with className containing "badge badge-amber"
    - Test: StatusBadge with status="denied" renders a span with className containing "badge badge-red"
    - Test: StatusBadge with status="in-transit" renders a span with className containing "badge badge-blue"
    - Test: StatusBadge with status="completed" renders a span with className containing "badge badge-muted"
    - Test: StatusBadge with status="project" renders a span with className containing "badge badge-muted"
    - Test: StatusBadge with unknown status falls back to "badge badge-muted"
    - Test: StatusBadge calls t() with the status string for display text
  </behavior>
  <action>
    Write test file FIRST (RED), then implement component (GREEN).

    **StatusBadge.jsx** — Pure React wrapper, no new CSS needed:
    - Props: `{ status, t, className }` (className allows callers to add extra classes)
    - Internal map: `{ approved: "badge-green", pending: "badge-amber", denied: "badge-red", "in-transit": "badge-blue", completed: "badge-muted", project: "badge-muted" }`
    - Renders: `<span className={\`badge \${cls} \${className || ''}\`.trim()}>{t ? t(status) : status}</span>`
    - No hardcoded hex values. No inline styles. No font-size — inherits from `.badge` class.

    Export from barrel: add `export { StatusBadge } from './StatusBadge';` to index.js.
  </action>
  <verify>
    <automated>npx vitest run src/components/field/StatusBadge.test.jsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - StatusBadge.jsx exports a named function `StatusBadge`
    - StatusBadge.jsx contains `STATUS_CLASS_MAP` or equivalent object mapping 6 status strings to badge-* classes
    - StatusBadge.jsx does NOT contain any hex color literals (grep -c "#[0-9a-fA-F]" returns 0)
    - StatusBadge.jsx renders a `<span>` with `className` containing `badge` and the mapped variant class
    - StatusBadge.test.jsx has at least 6 test cases covering all status values
    - `npx vitest run src/components/field/StatusBadge.test.jsx` exits 0
    - src/components/field/index.js contains `export { StatusBadge }`
  </acceptance_criteria>
  <done>StatusBadge maps all 6 status values to existing badge-* classes, all tests pass, exported from barrel</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build LoadingSpinner and Skeleton components (COMP-08)</name>
  <files>src/components/field/LoadingSpinner.jsx, src/components/field/LoadingSpinner.test.jsx</files>
  <read_first>
    - src/styles.js (confirm @keyframes spin and .animate-spin exist after Plan 01, confirm .skeleton-shimmer class)
    - .planning/phases/02-shared-field-components/02-UI-SPEC.md (LoadingSpinner/Skeleton section)
  </read_first>
  <behavior>
    - Test: LoadingSpinner renders an svg element (Lucide Loader2 renders as svg)
    - Test: LoadingSpinner has aria-label "Loading" (for screen readers)
    - Test: LoadingSpinner default size renders with width and height of 20
    - Test: Skeleton renders a div with className containing "skeleton-shimmer"
    - Test: Skeleton accepts width and height props and applies them as inline style
    - Test: Skeleton accepts className prop for additional styling
  </behavior>
  <action>
    Write test file FIRST (RED), then implement (GREEN).

    **LoadingSpinner.jsx** exports two components:

    1. `LoadingSpinner({ size = 20, className, t })`:
       - Renders: `<Loader2 size={size} className={\`animate-spin \${className || ''}\`.trim()} style={{ color: 'var(--amber)' }} aria-label={t ? t("Loading") : "Loading"} aria-hidden="false" />`
       - Import `Loader2` from `lucide-react`
       - The `color: var(--amber)` uses a CSS variable — this is NOT a hex literal, it's theme-aware.

    2. `Skeleton({ width, height, className, style })`:
       - Renders: `<div className={\`skeleton-shimmer \${className || ''}\`.trim()} style={{ width, height, ...style }} aria-hidden="true" />`
       - Width/height default to `'100%'` and `'1em'` respectively.

    Export both from barrel: add `export { LoadingSpinner, Skeleton } from './LoadingSpinner';` to index.js.
  </action>
  <verify>
    <automated>npx vitest run src/components/field/LoadingSpinner.test.jsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - LoadingSpinner.jsx exports named functions `LoadingSpinner` and `Skeleton`
    - LoadingSpinner.jsx imports `Loader2` from `lucide-react`
    - LoadingSpinner.jsx does NOT contain any hex color literals
    - LoadingSpinner renders with `aria-label` of "Loading"
    - Skeleton renders a div with className `skeleton-shimmer`
    - `npx vitest run src/components/field/LoadingSpinner.test.jsx` exits 0
    - src/components/field/index.js contains `export { LoadingSpinner, Skeleton }`
  </acceptance_criteria>
  <done>LoadingSpinner shows spinning Loader2 in amber, Skeleton shows shimmer block, both theme-aware, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build FieldButton component (COMP-01)</name>
  <files>src/components/field/FieldButton.jsx, src/components/field/FieldButton.test.jsx</files>
  <read_first>
    - src/styles.js (lines 96-113 — .btn, .btn-primary, .btn-ghost, .btn-danger, .touch-target, .focus-visible)
    - src/components/field/LoadingSpinner.jsx (just created — Loader2 import pattern)
    - .planning/phases/02-shared-field-components/02-UI-SPEC.md (FieldButton section)
    - .planning/phases/02-shared-field-components/02-RESEARCH.md (FieldButton code example)
  </read_first>
  <behavior>
    - Test: FieldButton default variant renders with className containing "btn btn-primary touch-target focus-visible"
    - Test: FieldButton variant="ghost" renders with className containing "btn btn-ghost"
    - Test: FieldButton variant="danger" renders with className containing "btn btn-danger"
    - Test: FieldButton with loading=true renders Loader2 svg instead of children text
    - Test: FieldButton with loading=true has aria-label "Loading, please wait"
    - Test: FieldButton with loading=true is disabled (button element has disabled attribute)
    - Test: FieldButton with disabled=true has disabled attribute and opacity style or class
    - Test: FieldButton passes onClick to underlying button element
    - Test: FieldButton renders children text when not loading
  </behavior>
  <action>
    Write test file FIRST (RED), then implement (GREEN).

    **FieldButton.jsx**:
    - Props: `{ variant = "primary", loading = false, disabled = false, children, className, t, ...props }`
    - Variant class map: `{ primary: "btn-primary", ghost: "btn-ghost", danger: "btn-danger" }`
    - Renders:
      ```jsx
      <button
        className={`btn ${variantClass} touch-target focus-visible ${className || ''}`.trim()}
        disabled={disabled || loading}
        aria-label={loading ? (t ? t("Loading, please wait") : "Loading, please wait") : undefined}
        style={disabled || loading ? { opacity: loading ? 0.7 : 0.45, pointerEvents: 'none' } : undefined}
        {...props}
      >
        {loading ? <Loader2 size={13} className="animate-spin" aria-hidden="true" /> : children}
      </button>
      ```
    - Import `Loader2` from `lucide-react` (do NOT import from LoadingSpinner — direct Lucide import keeps FieldButton self-contained and avoids a circular dependency risk)
    - NEVER apply `.btn-sm` alongside `.touch-target` — they conflict (see Pitfall 2 in RESEARCH.md)

    Export from barrel: add `export { FieldButton } from './FieldButton';` to index.js.
  </action>
  <verify>
    <automated>npx vitest run src/components/field/FieldButton.test.jsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - FieldButton.jsx exports a named function `FieldButton`
    - FieldButton.jsx imports `Loader2` from `lucide-react`
    - FieldButton.jsx does NOT contain any hex color literals (grep -c "#[0-9a-fA-F]" returns 0)
    - FieldButton.jsx renders a `<button>` element with className containing `btn`, `touch-target`, and `focus-visible`
    - FieldButton.jsx does NOT apply `btn-sm` class anywhere
    - Loading state replaces children with Loader2 and sets disabled on the button
    - `npx vitest run src/components/field/FieldButton.test.jsx` exits 0
    - src/components/field/index.js contains `export { FieldButton }`
  </acceptance_criteria>
  <done>FieldButton renders at 44px min-height in all 3 variants, shows spinner when loading, all tests pass, exported from barrel</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` runs all 3 test files and exits 0
- `grep -rn "#[0-9a-fA-F]" src/components/field/StatusBadge.jsx src/components/field/LoadingSpinner.jsx src/components/field/FieldButton.jsx` returns zero results
- `grep "export" src/components/field/index.js` shows StatusBadge, LoadingSpinner, Skeleton, FieldButton
</verification>

<success_criteria>
Three leaf components (StatusBadge, LoadingSpinner/Skeleton, FieldButton) are built, tested, and exported. All extend existing CSS classes via className composition. Zero hex literals in component source. All tests green. These components are ready for consumption by Plans 03-06.
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-field-components/02-02-SUMMARY.md`
</output>
