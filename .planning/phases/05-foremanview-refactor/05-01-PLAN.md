---
phase: 05-foremanview-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles.js
autonomous: true
requirements:
  - FRMN-01
  - FRMN-04
  - FRMN-06

must_haves:
  truths:
    - "Existing .foreman-kpi-value class uses var(--text-display) not 22px"
    - "Existing .foreman-kpi-label class uses var(--text-sm) not 9px"
    - "Existing .foreman-kpi-grid class uses gap: var(--space-2) not 10px"
    - "New frm-* CSS classes exist for all ForemanView UI regions"
    - "No hard-coded pixel values in new CSS class declarations"
    - "No hex color literals in new CSS class declarations"
  artifacts:
    - path: "src/styles.js"
      provides: "Updated FOREMAN VIEW section with tokenized existing classes + new frm-* classes"
      contains: "frm-team-row"
  key_links:
    - from: "src/styles.js"
      to: "src/tokens.css"
      via: "var(--text-display), var(--space-*), var(--touch-min) references"
      pattern: "var\\(--text-display\\)"
---

<objective>
Add all foreman-specific CSS classes to styles.js and update the 8 existing `.foreman-*` classes to consume design tokens instead of hard-coded values.

Purpose: Plans 2-4 need these CSS classes to replace 583 inline styles in ForemanView.jsx. Without the CSS vocabulary, JSX plans cannot eliminate inline styles.
Output: Updated FOREMAN VIEW section in src/styles.js with ~100+ tokenized CSS classes covering all 13 tab regions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foremanview-refactor/05-CONTEXT.md
@.planning/phases/05-foremanview-refactor/05-UI-SPEC.md
@.planning/phases/05-foremanview-refactor/05-RESEARCH.md

<interfaces>
<!-- Existing foreman classes in src/styles.js (lines 1042-1061) that MUST be updated in-place: -->
.foreman-kpi-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:16px}
.foreman-kpi-card{padding:14px;border-radius:var(--radius);border:1px solid var(--glass-border);background:var(--bg2)}
.foreman-kpi-label{font-size:9px;text-transform:uppercase;letter-spacing:0.8px;color:var(--text3);margin-bottom:4px}
.foreman-kpi-value{font-size:22px;font-weight:700;color:var(--amber);font-family:var(--font-mono)}
.foreman-kpi-sub{font-size:11px;color:var(--text2);margin-top:2px}
.foreman-team-row{display:flex;align-items:center;justify-content:space-between;padding:10px 14px;border-bottom:1px solid var(--glass-border)}
.foreman-team-name{font-size:13px;font-weight:600;color:var(--text)}
.foreman-team-role{font-size:11px;color:var(--text2)}
.foreman-team-hours{font-size:13px;font-family:var(--font-mono);color:var(--amber)}
.foreman-budget-bar{height:10px;border-radius:5px;background:var(--bg4);overflow:hidden;margin:8px 0}
.foreman-budget-fill{height:100%;border-radius:5px;transition:width 0.4s ease}
.foreman-project-select{width:100%;padding:8px 12px;border-radius:var(--radius-sm);border:1px solid var(--glass-border);background:var(--bg2);color:var(--text);font-size:14px}
.foreman-cost-row{display:flex;align-items:center;justify-content:space-between;padding:8px 0;border-bottom:1px solid var(--glass-border)}
.foreman-cost-row:last-child{border-bottom:none}

<!-- Responsive override at line 861: -->
@media(min-width:768px) .foreman-kpi-grid{grid-template-columns:repeat(4,1fr)}
@media(min-width:768px) .foreman-project-select{max-width:500px;margin:0 auto 16px;display:block;font-size:14px;padding:10px 14px}

<!-- Token references from tokens.css: -->
--text-display: 28px
--text-lg: 18px
--text-base: 13px
--text-sm: 11px
--space-1: 4px; --space-2: 8px; --space-3: 12px; --space-4: 16px; --space-6: 24px; --space-8: 32px
--touch-min: 44px
--radius, --radius-sm, --radius-lg (border radii)
--transition-micro: 150ms; --transition-state: 300ms
--weight-bold: 700; --weight-normal: 400
--leading-tight: 1.2; --leading-normal: 1.5
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing .foreman-* classes to consume design tokens</name>
  <files>src/styles.js</files>
  <read_first>
    - src/styles.js (current FOREMAN VIEW section, lines 1042-1061 and responsive override at line 861)
    - src/tokens.css (token values for reference)
    - .planning/phases/05-foremanview-refactor/05-UI-SPEC.md (typography and spacing contracts)
  </read_first>
  <action>
Update the existing FOREMAN VIEW section in src/styles.js (around line 1042). Do NOT create duplicate classes — update in-place.

Specific changes to existing classes:

1. `.foreman-kpi-grid` — change `gap:10px` to `gap:var(--space-2)`, change `margin-bottom:16px` to `margin-bottom:var(--space-4)`
2. `.foreman-kpi-card` — change `padding:14px` to `padding:var(--space-3)`
3. `.foreman-kpi-label` — change `font-size:9px` to `font-size:var(--text-sm)`, change `margin-bottom:4px` to `margin-bottom:var(--space-1)`
4. `.foreman-kpi-value` — change `font-size:22px` to `font-size:var(--text-display)`, add `line-height:var(--leading-tight)`
5. `.foreman-kpi-sub` — change `font-size:11px` to `font-size:var(--text-sm)`, change `margin-top:2px` to `margin-top:var(--space-1)`
6. `.foreman-team-row` — change `padding:10px 14px` to `padding:var(--space-2) var(--space-3)`, add `min-height:var(--touch-min)` (FRMN-03)
7. `.foreman-team-name` — change `font-size:13px` to `font-size:var(--text-base)`, change `font-weight:600` to `font-weight:var(--weight-bold)`
8. `.foreman-team-role` — change `font-size:11px` to `font-size:var(--text-sm)`
9. `.foreman-team-hours` — change `font-size:13px` to `font-size:var(--text-base)`
10. `.foreman-budget-bar` — change `margin:8px 0` to `margin:var(--space-2) 0`
11. `.foreman-project-select` — change `padding:8px 12px` to `padding:var(--space-2) var(--space-3)`, change `font-size:14px` to `font-size:var(--text-base)`
12. `.foreman-cost-row` — change `padding:8px 0` to `padding:var(--space-2) 0`

Also update the responsive override at line 861:
- `.foreman-project-select` responsive: change `font-size:14px` to `font-size:var(--text-base)`, change `padding:10px 14px` to `padding:var(--space-3) var(--space-3)`
  </action>
  <verify>
    <automated>grep "font-size:22px\|font-size:9px\|font-size:11px\|gap:10px\|font-size:14px" src/styles.js | grep -i foreman | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep "foreman-kpi-value" src/styles.js output contains "var(--text-display)"
    - grep "foreman-kpi-label" src/styles.js output contains "var(--text-sm)"
    - grep "foreman-kpi-grid" src/styles.js output contains "var(--space-2)"
    - grep "foreman-team-row" src/styles.js output contains "var(--touch-min)"
    - grep "foreman-project-select" src/styles.js output contains "var(--text-base)"
    - No hard-coded pixel font-size values remain in any .foreman-* class
  </acceptance_criteria>
  <done>All 8 existing .foreman-* classes updated to consume design tokens. Zero hard-coded pixel values in font-size/gap/padding properties.</done>
</task>

<task type="auto">
  <name>Task 2: Add new frm-* CSS classes for all ForemanView UI regions</name>
  <files>src/styles.js</files>
  <read_first>
    - src/styles.js (full FOREMAN VIEW section after Task 1 updates)
    - src/tabs/ForemanView.jsx (scan inline style patterns across all 13 tabs — read lines 1-200, 800-950, 1185-1300, 1646-1700, 1836-1900, 1936-2000, 2822-2900, 3004-3050, 3087-3150, 3555-3600, 3638-3680, 3702-3750, 3900-3953)
    - .planning/phases/05-foremanview-refactor/05-UI-SPEC.md (CSS class naming convention and region map)
    - .planning/phases/05-foremanview-refactor/05-RESEARCH.md (Pattern 2 naming convention, Pitfall 5 on volume)
  </read_first>
  <action>
Append new frm-* CSS classes below the existing .foreman-* classes in the FOREMAN VIEW section of styles.js. All new classes MUST use CSS custom property tokens — zero hard-coded px values, zero hex colors.

Add classes for each UI region following frm-{region}-{element} naming. The executor MUST scan ForemanView.jsx inline styles to derive the complete class set. Below are the known regions and minimum classes per the UI-SPEC:

**frm-content (layout):**
- `.frm-content-pad` — `padding-bottom:calc(56px + env(safe-area-inset-bottom) + var(--space-4))` (bottom padding for PortalTabBar + notched phones, same pattern as driver-content-pad)

**frm-kpi (dashboard KPI extensions beyond existing .foreman-kpi-*):**
- `.frm-kpi-trend` — `font-size:var(--text-sm);color:var(--text2)` (trend arrows/percentages)
- `.frm-kpi-icon` — `width:var(--space-6);height:var(--space-6);color:var(--amber)` (KPI card icons)

**frm-budget (budget section):**
- `.frm-budget-row` — `display:flex;align-items:center;justify-content:space-between;padding:var(--space-2) 0`
- `.frm-budget-label` — `font-size:var(--text-base);color:var(--text)`
- `.frm-budget-value` — `font-size:var(--text-base);font-family:var(--font-mono);color:var(--amber)`

**frm-team (team tab extensions beyond existing .foreman-team-*):**
- `.frm-team-status` — `font-size:var(--text-sm);color:var(--text2)`
- `.frm-team-clock-btn` — `min-height:var(--touch-min);padding:var(--space-2) var(--space-3);color:var(--amber)` (clock action buttons — NOT var(--accent), which is undefined)
- `.frm-team-section` — `margin-bottom:var(--space-4)`
- `.frm-team-header` — `font-size:var(--text-lg);font-weight:var(--weight-bold);color:var(--text);margin-bottom:var(--space-2)`

**frm-hours (hours tab):**
- `.frm-hours-grid` — `display:grid;gap:var(--space-1)`
- `.frm-hours-row` — `display:flex;align-items:center;justify-content:space-between;padding:var(--space-2) var(--space-3);min-height:var(--touch-min);border-bottom:1px solid var(--glass-border)`
- `.frm-hours-name` — `font-size:var(--text-base);font-weight:var(--weight-bold);color:var(--text)`
- `.frm-hours-value` — `font-size:var(--text-base);font-family:var(--font-mono);color:var(--amber)`
- `.frm-hours-day` — `font-size:var(--text-sm);text-align:center;min-width:var(--space-8)`
- `.frm-hours-total` — `font-size:var(--text-base);font-weight:var(--weight-bold);font-family:var(--font-mono);color:var(--amber)`
- `.frm-hours-input` — `width:var(--space-10);text-align:center;font-size:var(--text-sm);font-family:var(--font-mono);padding:var(--space-1);border:1px solid var(--glass-border);border-radius:var(--radius-sm);background:var(--bg2);color:var(--text)`
- `.frm-hours-header` — `font-size:var(--text-sm);color:var(--text3);text-align:center`

**frm-mat (materials tab):**
- `.frm-mat-actions` — `display:flex;gap:var(--space-2);margin-top:var(--space-2)`
- `.frm-mat-filter` — `display:flex;gap:var(--space-2);margin-bottom:var(--space-3);flex-wrap:wrap`

**frm-jsa (JSA tab — extend emp-jsa-* where structure matches, add frm-jsa-* for foreman-only elements):**
- `.frm-jsa-matrix` — `display:grid;grid-template-columns:auto 1fr 1fr 1fr;gap:var(--space-1);font-size:var(--text-sm)`
- `.frm-jsa-risk-cell` — `padding:var(--space-1) var(--space-2);border-radius:var(--radius-sm);text-align:center;font-size:var(--text-sm);font-weight:var(--weight-bold)`
- `.frm-jsa-row` — `display:flex;align-items:center;gap:var(--space-2);padding:var(--space-2) 0;border-bottom:1px solid var(--glass-border)`
- `.frm-jsa-rollcall-row` — `display:flex;align-items:center;gap:var(--space-2);padding:var(--space-2) var(--space-3);min-height:var(--touch-min)`
- `.frm-jsa-section` — `margin-bottom:var(--space-4)`
- `.frm-jsa-toggle` — `display:flex;gap:var(--space-2);margin-bottom:var(--space-3)`
- `.frm-jsa-ppe-grid` — `display:grid;grid-template-columns:repeat(auto-fill,minmax(120px,1fr));gap:var(--space-2)`
- `.frm-jsa-ppe-item` — `display:flex;align-items:center;gap:var(--space-2);padding:var(--space-2);border-radius:var(--radius-sm);border:1px solid var(--glass-border);min-height:var(--touch-min);cursor:pointer`
- `.frm-jsa-step` — `padding:var(--space-3);border:1px solid var(--glass-border);border-radius:var(--radius);margin-bottom:var(--space-2);background:var(--bg2)`
- `.frm-jsa-controls` — `display:flex;gap:var(--space-2);margin-bottom:var(--space-3)`

**frm-draw (drawings tab):**
- `.frm-draw-list` — `display:flex;flex-direction:column;gap:var(--space-1)`
- `.frm-draw-item` — `display:flex;align-items:center;justify-content:space-between;padding:var(--space-3);min-height:var(--touch-min);border-bottom:1px solid var(--glass-border);cursor:pointer`
- `.frm-draw-name` — `font-size:var(--text-base);color:var(--text);font-weight:var(--weight-bold)`
- `.frm-draw-meta` — `font-size:var(--text-sm);color:var(--text2)`
- `.frm-draw-revision-badge` — `font-size:var(--text-sm);padding:var(--space-1) var(--space-2);border-radius:var(--radius-sm);background:var(--bg3);color:var(--text2)`
- `.frm-draw-viewer` — `position:relative;width:100%;overflow:auto;background:var(--bg);border-radius:var(--radius)`
- `.frm-draw-toolbar` — `display:flex;gap:var(--space-2);padding:var(--space-2);background:var(--bg2);border-radius:var(--radius-sm);margin-bottom:var(--space-2)`

**frm-look (lookahead tab):**
- `.frm-look-grid` — `display:grid;grid-template-columns:repeat(5,1fr);gap:var(--space-1)`
- `.frm-look-day` — `padding:var(--space-2);min-height:var(--touch-min);border:1px solid var(--glass-border);border-radius:var(--radius-sm);background:var(--bg2);font-size:var(--text-sm)`
- `.frm-look-day-header` — `font-size:var(--text-sm);font-weight:var(--weight-bold);color:var(--text);text-align:center;padding:var(--space-1)`
- `.frm-look-task` — `font-size:var(--text-sm);padding:var(--space-1);border-radius:var(--radius-sm);background:var(--bg3);margin-bottom:var(--space-1)`
- `.frm-look-toolbar` — `display:flex;justify-content:space-between;align-items:center;margin-bottom:var(--space-3)`

**frm-report (reports/daily reports tab):**
- `.frm-report-header` — `display:flex;justify-content:space-between;align-items:center;margin-bottom:var(--space-3)`
- `.frm-report-section` — `margin-bottom:var(--space-4)`
- `.frm-report-section-title` — `font-size:var(--text-lg);font-weight:var(--weight-bold);color:var(--text);margin-bottom:var(--space-2)`
- `.frm-report-form` — `display:flex;flex-direction:column;gap:var(--space-3)`
- `.frm-report-row` — `display:flex;gap:var(--space-2);align-items:center`
- `.frm-report-label` — `font-size:var(--text-sm);color:var(--text3);min-width:80px`
- `.frm-report-value` — `font-size:var(--text-base);color:var(--text)`
- `.frm-report-weather` — `display:flex;gap:var(--space-2);flex-wrap:wrap`
- `.frm-report-weather-btn` — `padding:var(--space-2) var(--space-3);border-radius:var(--radius-sm);border:1px solid var(--glass-border);background:var(--bg2);color:var(--text);min-height:var(--touch-min);cursor:pointer`
- `.frm-report-weather-btn.active` — `background:var(--amber);color:var(--bg);border-color:var(--amber)`
- `.frm-report-crew-row` — `display:flex;align-items:center;justify-content:space-between;padding:var(--space-2);border-bottom:1px solid var(--glass-border)`
- `.frm-report-sig` — `margin-top:var(--space-4)`
- `.frm-report-list-item` — `padding:var(--space-3);border-bottom:1px solid var(--glass-border);cursor:pointer;min-height:var(--touch-min)`
- `.frm-report-date` — `font-size:var(--text-sm);color:var(--text2)`
- `.frm-report-status` — `font-size:var(--text-sm)`

**frm-doc (documents tab):**
- `.frm-doc-list` — `display:flex;flex-direction:column;gap:var(--space-1)`
- `.frm-doc-item` — `display:flex;align-items:center;gap:var(--space-3);padding:var(--space-3);min-height:var(--touch-min);border-bottom:1px solid var(--glass-border);cursor:pointer`
- `.frm-doc-icon` — `width:var(--space-8);height:var(--space-8);display:flex;align-items:center;justify-content:center;border-radius:var(--radius-sm);background:var(--bg3);color:var(--amber)`
- `.frm-doc-name` — `font-size:var(--text-base);color:var(--text);font-weight:var(--weight-bold)`
- `.frm-doc-meta` — `font-size:var(--text-sm);color:var(--text2)`

**frm-site (site tab):**
- `.frm-site-section` — `margin-bottom:var(--space-4)`
- `.frm-site-photo-grid` — `display:grid;grid-template-columns:repeat(3,1fr);gap:var(--space-2)`
- `.frm-site-photo` — `aspect-ratio:1;border-radius:var(--radius-sm);overflow:hidden;background:var(--bg3)`

**frm-notes (notes tab):**
- `.frm-notes-list` — `display:flex;flex-direction:column;gap:var(--space-2)`
- `.frm-notes-item` — `padding:var(--space-3);border:1px solid var(--glass-border);border-radius:var(--radius);background:var(--bg2)`
- `.frm-notes-pinned` — `border-left:3px solid var(--amber)`
- `.frm-notes-date` — `font-size:var(--text-sm);color:var(--text2);margin-top:var(--space-1)`
- `.frm-notes-text` — `font-size:var(--text-base);color:var(--text);white-space:pre-wrap`
- `.frm-notes-toolbar` — `display:flex;gap:var(--space-2);margin-bottom:var(--space-3)`
- `.frm-notes-input` — `width:100%;min-height:80px;padding:var(--space-3);border:1px solid var(--glass-border);border-radius:var(--radius);background:var(--bg2);color:var(--text);font-size:var(--text-base);font-family:var(--font-body);resize:vertical`

**frm-settings (settings extensions):**
- `.frm-settings-section` — `margin-bottom:var(--space-4)`
- `.frm-settings-label` — `font-size:var(--text-sm);color:var(--text3);margin-bottom:var(--space-1)`

**frm-clock (clock tab):**
- `.frm-clock-status` — `text-align:center;padding:var(--space-4)`
- `.frm-clock-time` — `font-size:var(--text-display);font-family:var(--font-mono);color:var(--amber);font-weight:var(--weight-bold)`
- `.frm-clock-project-search` — `margin-bottom:var(--space-3)`
- `.frm-clock-project-list` — `display:flex;flex-direction:column;gap:var(--space-1)`
- `.frm-clock-project-item` — `padding:var(--space-3);border:1px solid var(--glass-border);border-radius:var(--radius);cursor:pointer;min-height:var(--touch-min)`
- `.frm-clock-project-item.selected` — `border-color:var(--amber);background:var(--bg3)`

**frm-phase (phase status colors):**
- `.frm-phase-active` — `color:var(--phase-active)` (FRMN-06)
- `.frm-phase-estimating` — `color:var(--phase-estimating)` (FRMN-06)
- `.frm-phase-pre-construction` — `color:var(--phase-pre-construction)` (FRMN-06)
- `.frm-phase-completed` — `color:var(--phase-completed)` (FRMN-06)
- `.frm-phase-warranty` — `color:var(--phase-warranty)` (FRMN-06)
- `.frm-phase-in-progress` — `color:var(--phase-in-progress)` (FRMN-06)
- `.frm-phase-dot` — `width:var(--space-2);height:var(--space-2);border-radius:50%;display:inline-block`
- `.frm-phase-dot.active` — `background:var(--phase-active)` (FRMN-06)
- `.frm-phase-dot.estimating` — `background:var(--phase-estimating)`
- `.frm-phase-dot.pre-construction` — `background:var(--phase-pre-construction)`
- `.frm-phase-dot.completed` — `background:var(--phase-completed)`
- `.frm-phase-dot.warranty` — `background:var(--phase-warranty)`
- `.frm-phase-dot.in-progress` — `background:var(--phase-in-progress)`

**General utility classes:**
- `.frm-section-title` — `font-size:var(--text-lg);font-weight:var(--weight-bold);color:var(--text);margin-bottom:var(--space-2)`
- `.frm-section-sub` — `font-size:var(--text-sm);color:var(--text2);margin-bottom:var(--space-3)`
- `.frm-flex-between` — `display:flex;align-items:center;justify-content:space-between`
- `.frm-flex-gap` — `display:flex;gap:var(--space-2);align-items:center`
- `.frm-mono` — `font-family:var(--font-mono)`
- `.frm-amber` — `color:var(--amber)`
- `.frm-muted` — `color:var(--text2)`
- `.frm-divider` — `border-bottom:1px solid var(--glass-border);margin:var(--space-2) 0`
- `.frm-search-bar` — `display:flex;gap:var(--space-2);margin-bottom:var(--space-3)`
- `.frm-login-wrap` — `max-width:340px;margin:0 auto;padding:var(--space-8) var(--space-4)`
- `.frm-login-title` — `font-size:var(--text-lg);font-weight:var(--weight-bold);color:var(--text);text-align:center;margin-bottom:var(--space-6)`

NOTE: Plans 2-4 may add additional incidental frm-* classes to styles.js as they encounter inline styles not covered here. This is expected per RESEARCH.md Pitfall 5.
  </action>
  <verify>
    <automated>grep -c "frm-\|\.foreman-" src/styles.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "frm-content-pad" src/styles.js returns a match
    - grep "frm-jsa-matrix" src/styles.js returns a match
    - grep "frm-report-header" src/styles.js returns a match
    - grep "frm-draw-item" src/styles.js returns a match
    - grep "frm-look-grid" src/styles.js returns a match
    - grep "frm-notes-item" src/styles.js returns a match
    - grep "frm-phase-active" src/styles.js returns a match
    - grep "frm-clock-time" src/styles.js returns a match
    - grep -c "frm-" src/styles.js returns at least 70 (new classes)
    - No hex color literals (#xxxxxx) in any new frm-* class declarations
    - No hard-coded px values in font-size of new frm-* class declarations (all use var(--text-*))
  </acceptance_criteria>
  <done>styles.js FOREMAN VIEW section contains all region-level CSS classes for 13 tabs. All classes consume design tokens exclusively. Plans 2-4 can reference these classes to eliminate inline styles.</done>
</task>

</tasks>

<verification>
1. `grep "font-size:22px\|font-size:9px\|gap:10px" src/styles.js | grep -i foreman` returns 0 lines
2. `grep -c "frm-" src/styles.js` returns at least 70
3. `grep "var(--text-display)" src/styles.js | grep foreman-kpi-value` returns a match
4. `grep "#[0-9a-fA-F]\{6\}" src/styles.js | grep "frm-"` returns 0 lines (no hex in new classes)
</verification>

<success_criteria>
- All 8 existing .foreman-* classes updated to consume design tokens (zero hard-coded px in font-size/gap/padding)
- At least 70 new frm-* CSS classes added covering all 13 tab UI regions
- All new classes use only CSS custom property tokens — zero hex, zero px
- Phase color utility classes (.frm-phase-*) use var(--phase-*) tokens (FRMN-06)
- KPI value class uses var(--text-display) per D-05 (FRMN-04)
</success_criteria>

<output>
After completion, create `.planning/phases/05-foremanview-refactor/05-01-SUMMARY.md`
</output>
