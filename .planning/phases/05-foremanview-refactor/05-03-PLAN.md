---
phase: 05-foremanview-refactor
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/tabs/ForemanView.jsx
  - src/styles.js
autonomous: true
requirements:
  - FRMN-01
  - FRMN-03
  - FRMN-05

must_haves:
  truths:
    - "Materials tab uses MaterialRequestCard from shared library"
    - "Materials tab uses AsyncState for loading/empty/error states"
    - "JSA tab uses FieldSignaturePad from barrel import for supervisor sign-off"
    - "JSA hazard matrix uses frm-jsa-matrix CSS class, not inline styles"
    - "JSA PPE grid uses frm-jsa-ppe-grid/frm-jsa-ppe-item classes"
    - "All buttons in materials and JSA tabs use FieldButton"
  artifacts:
    - path: "src/tabs/ForemanView.jsx"
      provides: "Materials and JSA tabs refactored with zero static inline styles"
    - path: "src/styles.js"
      provides: "Any additional frm-jsa-* or frm-mat-* classes needed beyond Plan 1"
  key_links:
    - from: "src/tabs/ForemanView.jsx"
      to: "src/components/field/MaterialRequestCard.jsx"
      via: "import from barrel, used in materials tab"
      pattern: "MaterialRequestCard"
    - from: "src/tabs/ForemanView.jsx"
      to: "src/components/field/FieldSignaturePad.jsx"
      via: "import from barrel, used in JSA supervisor sign-off"
      pattern: "FieldSignaturePad"
---

<objective>
Refactor ForemanView.jsx lines ~1836-2821: eliminate all inline styles from Materials and JSA tabs. Wire MaterialRequestCard, FieldSignaturePad (from barrel), AsyncState, FieldButton, FieldInput, FieldSelect, EmptyState, and StatusBadge.

Purpose: Materials and JSA are the most complex tabs with the highest inline style density. JSA has foreman-specific elements (hazard matrix, roll call, supervisor sign-off) not present in EmployeeView.
Output: Materials and JSA tabs refactored. Inline style count drops by ~180.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foremanview-refactor/05-CONTEXT.md
@.planning/phases/05-foremanview-refactor/05-UI-SPEC.md
@.planning/phases/05-foremanview-refactor/05-RESEARCH.md
@.planning/phases/05-foremanview-refactor/05-01-SUMMARY.md
@.planning/phases/05-foremanview-refactor/05-02-SUMMARY.md

<interfaces>
<!-- MaterialRequestCard props (from Phase 2): -->
<MaterialRequestCard
  request={requestObject}   // { id, item, quantity, unit, status, requestedBy, date, notes }
  onApprove={fn}            // optional — foreman can approve
  onReject={fn}             // optional — foreman can reject
  onConfirmReceipt={fn}     // optional — foreman confirms delivery
  t={fn}
/>

<!-- FieldSignaturePad props (from barrel — same API as deleted inline): -->
<FieldSignaturePad
  onSave={fn}    // receives { getSig, clear } ref-like object
  onClear={fn}   // optional
  label={string} // optional label above canvas
  t={fn}
/>

<!-- AsyncState props: -->
<AsyncState loading={bool} empty={bool} error={string|null}
  emptyIcon={LucideIcon} emptyMessage={string} t={fn} skeleton={ReactNode}>
  {children}
</AsyncState>

<!-- FieldInput props: -->
<FieldInput label={string} value={string} onChange={fn} placeholder={string}
  inputMode="text|numeric|decimal" error={string} disabled={bool} t={fn} />

<!-- FieldSelect props: -->
<FieldSelect label={string} value={string} onChange={fn} options={[{value,label}]}
  disabled={bool} t={fn} />

<!-- StatusBadge props: -->
<StatusBadge status="pending|approved|rejected|completed|active" t={fn} />

<!-- EmptyState copywriting (UI-SPEC): -->
Materials empty: heading=t("No Requests"), message=t("No material requests for this project.")
JSA empty: heading=t("No JSA"), message=t("No Job Safety Analysis on file. Tap New JSA to begin.")

<!-- ForemanView JSA section boundaries: -->
Line 1836: foremanTab === "materials"
Line 1936: foremanTab === "jsa"
Line ~2821: end of JSA section

<!-- CSS classes available from Plan 1: -->
frm-mat-actions, frm-mat-filter
frm-jsa-matrix, frm-jsa-risk-cell, frm-jsa-row, frm-jsa-rollcall-row
frm-jsa-section, frm-jsa-toggle, frm-jsa-ppe-grid, frm-jsa-ppe-item
frm-jsa-step, frm-jsa-controls
frm-section-title, frm-section-sub, frm-flex-between, frm-flex-gap
frm-mono, frm-amber, frm-muted, frm-divider
emp-jsa-* classes (reuse where structure matches EmployeeView JSA)

<!-- Phase hex-to-token mapping (for any phase colors in materials/JSA): -->
#10b981 → var(--phase-active)
#3b82f6 → var(--phase-estimating)
#8b5cf6 → var(--phase-pre-construction)
#6b7280 → var(--phase-completed)
#eab308 → var(--phase-warranty)
#f59e0b → var(--phase-in-progress)

<!-- Undefined token mapping: -->
var(--accent) → var(--amber) via CSS class
var(--accent-dim) → var(--amber-dim)
var(--surface1) → var(--bg2)
var(--surface2) → var(--bg3)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Materials tab — wire MaterialRequestCard, AsyncState, eliminate inline styles</name>
  <files>src/tabs/ForemanView.jsx, src/styles.js</files>
  <read_first>
    - src/tabs/ForemanView.jsx (lines 1836-1935 — full materials tab section)
    - src/styles.js (FOREMAN VIEW section — frm-mat-* classes from Plan 1)
    - src/components/field/MaterialRequestCard.jsx (props and structure)
    - src/components/field/AsyncState.jsx (props)
    - .planning/phases/05-foremanview-refactor/05-UI-SPEC.md (copywriting contract for materials empty state, component wiring)
  </read_first>
  <action>
Refactor the materials tab section (~lines 1836-1935) to eliminate all inline styles.

**Material request list:**
- Wrap the material request list in `<AsyncState>` with:
  - `loading={false}` (material requests are synchronously available from app prop — no async fetch in current code)
  - `empty={(filteredRequests || []).length === 0}`
  - `emptyIcon={Package}`
  - `emptyMessage={t("No material requests for this project.")}`
  - `t={t}`
- Replace custom material request card markup with `<MaterialRequestCard>` component for each request. Pass the request object and foreman action handlers (approve, reject, confirmReceipt).
- If the current code has custom card markup with inline styles for each material request, replace entirely with MaterialRequestCard.
- If MaterialRequestCard does not perfectly match the existing data shape, adapt the request object mapping in the JSX (do NOT modify MaterialRequestCard.jsx).

**Action buttons:**
- Replace raw `<button>` elements for approve/reject/confirm with `<FieldButton>`:
  - Approve: `<FieldButton variant="primary" onClick={...}>{t("Approve")}</FieldButton>`
  - Reject: `<FieldButton variant="danger" onClick={...}>{t("Reject")}</FieldButton>`
  - Confirm Receipt: `<FieldButton variant="primary" onClick={...}>{t("Confirm Receipt")}</FieldButton>`
- If these actions are within MaterialRequestCard, they are already handled — verify.

**Filter/search bar:**
- Replace inline-styled filter controls with frm-mat-filter class.
- Use FieldSelect for status filter dropdown if present.
- Use FieldInput for search input if present.

**Inline style replacements:**
- All padding/margin → frm-* classes
- All fontSize → frm-section-title, frm-muted, etc.
- All color: "var(--accent)" → className with var(--amber)
- All hex colors → frm-phase-* classes or StatusBadge component

If any inline style needs a class not in Plan 1, add it to styles.js FOREMAN VIEW section following frm-mat-{element} naming.
  </action>
  <verify>
    <automated>sed -n '1836,1935p' src/tabs/ForemanView.jsx | grep -c "style={{"</automated>
  </verify>
  <acceptance_criteria>
    - grep "MaterialRequestCard" src/tabs/ForemanView.jsx returns at least 1 match
    - grep "AsyncState" src/tabs/ForemanView.jsx returns at least 1 match
    - Inline style count in lines 1836-1935 is 0 (or only dynamic exceptions with JSX comment)
    - grep "frm-mat-" src/tabs/ForemanView.jsx returns at least 1 match
    - No var(--accent) references remain in lines 1836-1935
  </acceptance_criteria>
  <done>Materials tab uses MaterialRequestCard, AsyncState, FieldButton. Zero static inline styles. All action buttons meet 44px touch target via FieldButton (FRMN-03).</done>
</task>

<task type="auto">
  <name>Task 2: Refactor JSA tab — hazard matrix, PPE grid, roll call, signature, eliminate inline styles</name>
  <files>src/tabs/ForemanView.jsx, src/styles.js</files>
  <read_first>
    - src/tabs/ForemanView.jsx (lines 1936-2821 — full JSA tab section)
    - src/styles.js (FOREMAN VIEW section — frm-jsa-* classes, plus EMPLOYEE VIEW section for emp-jsa-* classes to reuse)
    - src/components/field/FieldSignaturePad.jsx (verify onSave/onClear/label/t props match usage)
    - .planning/phases/05-foremanview-refactor/05-RESEARCH.md (Pitfall 4 — JSA foreman-specific vs shared emp-jsa structure)
    - .planning/phases/05-foremanview-refactor/05-UI-SPEC.md (copywriting for JSA empty state, interaction contract)
  </read_first>
  <action>
Refactor the JSA tab section (~lines 1936-2821) to eliminate all inline styles. This is the most complex section with foreman-specific elements not present in EmployeeView.

**JSA list view (jsaView === "list"):**
- Wrap in AsyncState:
  - `empty={(projectJsas || []).length === 0}`
  - `emptyIcon={Shield}`
  - `emptyMessage={t("No Job Safety Analysis on file. Tap New JSA to begin.")}`
- Use FieldCard for each JSA list item
- Use StatusBadge for JSA status
- Use FieldButton for "New JSA" action
- Replace inline padding/margin/fontSize with frm-jsa-* classes

**JSA create/detail view (jsaView === "create" or "detail"):**

A. **Form inputs:** Replace all inline-styled inputs with FieldInput/FieldSelect:
   - Trade selector: `<FieldSelect label={t("Trade")} options={...} />`
   - Template selector: FieldSelect
   - Location: FieldInput
   - Supervisor: FieldInput
   - Date: FieldInput with type="date"
   - Shift: FieldSelect with options day/night/swing

B. **Hazard matrix grid:** Replace inline grid styles with `frm-jsa-matrix` class. Each cell uses `frm-jsa-risk-cell`. Risk score color cells: use dynamic inline style ONLY for the computed background color from `riskColor()` function — this is a runtime-computed value: `style={{ backgroundColor: riskColor(score) }} {/* dynamic: risk score color computed at runtime */}`.

C. **PPE grid:** Replace inline grid with `frm-jsa-ppe-grid` class. Each PPE item uses `frm-jsa-ppe-item` class. Toggle state (selected/not) handled via className toggle, not inline style.

D. **Roll call section:** Replace inline styles with `frm-jsa-rollcall-row`. Each crew member row must have `min-height: var(--touch-min)` via the class (44px, FRMN-03). Use FieldButton for attendance toggle buttons.

E. **Steps/hazards section:** Each step uses `frm-jsa-step` class. Add/remove buttons use FieldButton.

F. **Supervisor sign-off:** Replace inline FieldSignaturePad call with the barrel-imported FieldSignaturePad (already imported in Plan 2). The API is identical: `<FieldSignaturePad onSave={...} label={t("Supervisor Signature")} t={t} />`. This satisfies FRMN-05.

G. **Indoor/outdoor toggle:** Replace inline-styled toggle buttons with frm-jsa-toggle class wrapper + FieldButton or styled buttons.

**Reuse strategy:**
- Use `emp-jsa-*` classes for list view, detail view layout, and sign-off sections that share structure with EmployeeView.
- Use `frm-jsa-*` classes for hazard matrix, roll call, indoor/outdoor toggle, PPE grid (foreman-specific).
- If the HTML structure matches EmployeeView exactly, use emp-jsa-* class.
- If it has foreman-only elements, use frm-jsa-* class.

**Inline style replacements (same rules as Plan 2 Task 2):**
- var(--accent) → var(--amber) via class
- var(--surface1) → var(--bg2)
- All fontSize → token-based class
- All hex colors → StatusBadge or frm-phase-* class
- KEEP only dynamic runtime values as inline style + JSX comment

If any inline style needs a class not in Plan 1, add it to styles.js FOREMAN VIEW section.
  </action>
  <verify>
    <automated>sed -n '1936,2821p' src/tabs/ForemanView.jsx | grep -c "style={{"</automated>
  </verify>
  <acceptance_criteria>
    - grep "FieldSignaturePad" src/tabs/ForemanView.jsx returns at least 1 match (barrel import used in JSA)
    - grep "frm-jsa-matrix" src/tabs/ForemanView.jsx returns at least 1 match
    - grep "frm-jsa-ppe-grid" src/tabs/ForemanView.jsx returns at least 1 match
    - grep "frm-jsa-rollcall-row" src/tabs/ForemanView.jsx returns at least 1 match
    - Inline style count in lines 1936-2821 is less than 10 (only dynamic risk color exceptions)
    - grep "FieldButton" src/tabs/ForemanView.jsx returns at least 10 matches total (cumulative from Plan 2 + this task)
    - grep "FieldInput\|FieldSelect" src/tabs/ForemanView.jsx returns at least 5 matches (JSA form fields)
    - No var(--accent) references remain in lines 1936-2821
  </acceptance_criteria>
  <done>Materials and JSA tabs fully refactored. FieldSignaturePad from barrel used for supervisor sign-off (FRMN-05). Hazard matrix, PPE grid, roll call use frm-jsa-* CSS classes. All buttons use FieldButton (FRMN-03). Only dynamic runtime values remain as inline styles.</done>
</task>

</tasks>

<verification>
1. `sed -n '1836,2821p' src/tabs/ForemanView.jsx | grep -c "style={{"` returns less than 10
2. `grep "MaterialRequestCard" src/tabs/ForemanView.jsx` returns matches
3. `grep "FieldSignaturePad" src/tabs/ForemanView.jsx` returns matches (barrel import, not inline definition)
4. `grep "function FieldSignaturePad" src/tabs/ForemanView.jsx` returns 0 (inline def gone from Plan 2)
5. `grep -c "style={{" src/tabs/ForemanView.jsx` returns less than 170 (cumulative)
</verification>

<success_criteria>
- Materials tab uses MaterialRequestCard and AsyncState from shared library (FRMN-05)
- JSA supervisor sign-off uses FieldSignaturePad from barrel (FRMN-05)
- JSA hazard matrix uses frm-jsa-matrix CSS class, not inline grid styles (FRMN-01)
- All buttons in materials/JSA meet 44px touch target via FieldButton (FRMN-03)
- Only dynamic runtime values (risk score colors) remain as inline styles
- Zero var(--accent), var(--surface1), var(--surface2) references remain
</success_criteria>

<output>
After completion, create `.planning/phases/05-foremanview-refactor/05-03-SUMMARY.md`
</output>
