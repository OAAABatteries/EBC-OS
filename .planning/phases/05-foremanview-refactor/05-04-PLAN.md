---
phase: 05-foremanview-refactor
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - src/tabs/ForemanView.jsx
  - src/styles.js
autonomous: true
requirements:
  - FRMN-01
  - FRMN-03
  - FRMN-05
  - FRMN-06

must_haves:
  truths:
    - "grep 'style={{' src/tabs/ForemanView.jsx returns only dynamic runtime exceptions"
    - "Daily reports section uses FieldSignaturePad from barrel for report signatures"
    - "Daily reports section uses FieldCard, FieldButton, FieldInput"
    - "Drawings tab uses frm-draw-* CSS classes"
    - "Lookahead tab uses frm-look-* CSS classes"
    - "Notes tab uses frm-notes-* CSS classes"
    - "No hex color literals (#xxxxxx) remain in ForemanView.jsx except in jsaConstants imports"
    - "All tabs show EmptyState when no data"
  artifacts:
    - path: "src/tabs/ForemanView.jsx"
      provides: "Fully refactored ForemanView — zero static inline styles across all 13 tabs"
    - path: "src/styles.js"
      provides: "Any additional frm-* classes needed for remaining tabs"
  key_links:
    - from: "src/tabs/ForemanView.jsx"
      to: "src/components/field/FieldSignaturePad.jsx"
      via: "barrel import used in daily reports signature"
      pattern: "FieldSignaturePad"
    - from: "src/tabs/ForemanView.jsx"
      to: "src/styles.js"
      via: "frm-draw-*, frm-look-*, frm-report-*, frm-doc-*, frm-site-*, frm-notes-* classes"
      pattern: "frm-draw-|frm-look-|frm-report-|frm-doc-|frm-site-|frm-notes-"
---

<objective>
Refactor ForemanView.jsx lines ~2822-3953: eliminate all remaining inline styles from drawings, lookahead, reports, documents, site, notes tabs. This is the final plan — after completion, ForemanView.jsx has zero static inline styles.

Purpose: Complete the ForemanView refactor. These 7 remaining tabs contain ~150 inline styles. Daily reports with signatures is the most complex section.
Output: ForemanView.jsx fully refactored. `grep "style={{" src/tabs/ForemanView.jsx` returns only dynamic runtime exceptions (budget %, progress %, risk colors, selected states).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foremanview-refactor/05-CONTEXT.md
@.planning/phases/05-foremanview-refactor/05-UI-SPEC.md
@.planning/phases/05-foremanview-refactor/05-RESEARCH.md
@.planning/phases/05-foremanview-refactor/05-01-SUMMARY.md
@.planning/phases/05-foremanview-refactor/05-02-SUMMARY.md
@.planning/phases/05-foremanview-refactor/05-03-SUMMARY.md

<interfaces>
<!-- Tab section boundaries for this plan: -->
Line 2822: foremanTab === "drawings"
Line 3004: foremanTab === "lookahead"
Line 3087: foremanTab === "reports" (daily reports — complex with signature, weather, crew)
Line 3555: foremanTab === "documents"
Line 3638: foremanTab === "site"
Line 3702: foremanTab === "notes"
Line 3953: end of file

<!-- CSS classes from Plan 1 available for these tabs: -->
frm-draw-list, frm-draw-item, frm-draw-name, frm-draw-meta, frm-draw-revision-badge, frm-draw-viewer, frm-draw-toolbar
frm-look-grid, frm-look-day, frm-look-day-header, frm-look-task, frm-look-toolbar
frm-report-header, frm-report-section, frm-report-section-title, frm-report-form, frm-report-row, frm-report-label, frm-report-value, frm-report-weather, frm-report-weather-btn, frm-report-crew-row, frm-report-sig, frm-report-list-item, frm-report-date, frm-report-status
frm-doc-list, frm-doc-item, frm-doc-icon, frm-doc-name, frm-doc-meta
frm-site-section, frm-site-photo-grid, frm-site-photo
frm-notes-list, frm-notes-item, frm-notes-pinned, frm-notes-date, frm-notes-text, frm-notes-toolbar, frm-notes-input
frm-section-title, frm-section-sub, frm-flex-between, frm-flex-gap, frm-mono, frm-amber, frm-muted, frm-divider

<!-- EmptyState copywriting (UI-SPEC): -->
Drawings: heading=t("No Drawings"), message=t("No drawings uploaded for this project.")
Lookahead: heading=t("No Schedule"), message=t("No lookahead schedule created yet.")
Reports: heading=t("No Reports"), message=t("Tap + New Report to create the first daily report.")
Documents: heading=t("No Documents"), message=t("No documents uploaded for this project.")
Notes: heading=t("No Notes"), message=t("No notes recorded for this project.")

<!-- Phase hex-to-token mapping: -->
#10b981 → var(--phase-active)
#3b82f6 → var(--phase-estimating)
#8b5cf6 → var(--phase-pre-construction)
#6b7280 → var(--phase-completed)
#eab308 → var(--phase-warranty)
#f59e0b → var(--phase-in-progress)

<!-- Undefined token mapping: -->
var(--accent) → var(--amber)
var(--accent-dim) → var(--amber-dim)
var(--surface1) → var(--bg2)
var(--surface2) → var(--bg3)
var(--orange, #f59e0b) → var(--amber) (drawings tab, remove fallback)
var(--purple, #8b5cf6) → var(--phase-pre-construction)

<!-- FieldSignaturePad — used in daily reports for foreman signature: -->
<FieldSignaturePad onSave={...} label={t("Foreman Signature")} t={t} />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor drawings, lookahead, and documents tabs</name>
  <files>src/tabs/ForemanView.jsx, src/styles.js</files>
  <read_first>
    - src/tabs/ForemanView.jsx (lines 2822-3087 for drawings and lookahead; lines 3555-3637 for documents)
    - src/styles.js (FOREMAN VIEW section — frm-draw-*, frm-look-*, frm-doc-* classes)
    - .planning/phases/05-foremanview-refactor/05-UI-SPEC.md (interaction contract for drawings touch targets, copywriting for empty states)
    - .planning/phases/05-foremanview-refactor/05-RESEARCH.md (Pattern 6 undefined token resolution — var(--orange) in drawings)
  </read_first>
  <action>
Refactor three tab sections to eliminate all inline styles.

**Drawings tab (~lines 2822-3003):**
- Drawing list items: replace inline styles with frm-draw-item class. Each item must have `min-height: var(--touch-min)` via the class (FRMN-03).
- Drawing name/meta: use frm-draw-name, frm-draw-meta classes.
- Revision badges: use frm-draw-revision-badge class.
- Drawing viewer container: use frm-draw-viewer class.
- Zoom toolbar: use frm-draw-toolbar class. Zoom buttons use FieldButton.
- Download/open buttons: use FieldButton.
- Replace `var(--orange, #f59e0b)` with `var(--amber)` — remove the hex fallback.
- Replace `var(--purple, #8b5cf6)` with `var(--phase-pre-construction)`.
- Empty state: `<EmptyState icon={Building2} heading={t("No Drawings")} message={t("No drawings uploaded for this project.")} t={t} />`
- Loading state: wrap in AsyncState if drawingsLoading state exists (it does — line 134).
- PdfViewer Suspense wrapper: keep the existing `<Suspense fallback={<LoadingSpinner />}>` pattern — it already uses the shared component.

**Lookahead tab (~lines 3004-3086):**
- Schedule grid: use frm-look-grid class.
- Day cells: use frm-look-day class. Each cell must have min-height for touch targets (44px via class, FRMN-03).
- Day headers: use frm-look-day-header class.
- Tasks within days: use frm-look-task class.
- Toolbar (week navigation, add button): use frm-look-toolbar class. Navigation buttons use FieldButton.
- Empty state: `<EmptyState icon={ClipboardList} heading={t("No Schedule")} message={t("No lookahead schedule created yet.")} t={t} />`

**Documents tab (~lines 3555-3637):**
- Document list: use frm-doc-list class.
- Each document item: use frm-doc-item class. Must have min-height: var(--touch-min) via class (FRMN-03).
- Document icon: use frm-doc-icon class.
- Document name/meta: use frm-doc-name, frm-doc-meta classes.
- Use FieldButton for download/view actions.
- Empty state: `<EmptyState icon={FileText} heading={t("No Documents")} message={t("No documents uploaded for this project.")} t={t} />`

**General inline style replacement rules (same as Plans 2-3):**
- All padding/margin → frm-* or shared utility classes
- All fontSize → token-based classes
- var(--accent) → var(--amber) via class
- var(--surface1/surface2) → var(--bg2/bg3) via class
- Hex colors → frm-phase-* or StatusBadge
- KEEP only dynamic runtime values as inline style + JSX comment `{/* dynamic: reason */}`

If any inline style needs a class not in Plan 1, add it to styles.js FOREMAN VIEW section.
  </action>
  <verify>
    <automated>bash -c "sed -n '2822,3087p' src/tabs/ForemanView.jsx | grep -c 'style={{'; sed -n '3555,3637p' src/tabs/ForemanView.jsx | grep -c 'style={{'"</automated>
  </verify>
  <acceptance_criteria>
    - grep "frm-draw-item" src/tabs/ForemanView.jsx returns at least 1 match
    - grep "frm-look-grid" src/tabs/ForemanView.jsx returns at least 1 match
    - grep "frm-doc-item" src/tabs/ForemanView.jsx returns at least 1 match
    - grep 'var(--orange' src/tabs/ForemanView.jsx returns 0 matches (replaced with var(--amber))
    - grep 'var(--purple' src/tabs/ForemanView.jsx returns 0 matches (replaced with var(--phase-pre-construction))
    - Inline style count in drawings+lookahead+documents combined is less than 5 (dynamic exceptions only)
    - grep "EmptyState" src/tabs/ForemanView.jsx returns at least 5 matches total (cumulative from all plans)
  </acceptance_criteria>
  <done>Drawings, lookahead, and documents tabs have zero static inline styles. All use frm-* CSS classes and shared components. Undefined tokens (--orange, --purple) eliminated. Documents empty state uses explicit copy: heading="No Documents", message="No documents uploaded for this project."</done>
</task>

<task type="auto">
  <name>Task 2: Refactor daily reports, site, and notes tabs — final inline style elimination</name>
  <files>src/tabs/ForemanView.jsx, src/styles.js</files>
  <read_first>
    - src/tabs/ForemanView.jsx (lines 3087-3554 for reports; lines 3638-3701 for site; lines 3702-3953 for notes)
    - src/styles.js (FOREMAN VIEW section — frm-report-*, frm-site-*, frm-notes-* classes)
    - src/components/field/FieldSignaturePad.jsx (verify API for report signature usage)
    - .planning/phases/05-foremanview-refactor/05-UI-SPEC.md (copywriting for reports/notes empty states, interaction contract for weather buttons, budget progress bar dynamic exception)
    - .planning/phases/05-foremanview-refactor/05-RESEARCH.md (Pattern 7 dynamic exceptions list)
  </read_first>
  <action>
Refactor the final three tab sections. After this task, `grep "style={{" src/tabs/ForemanView.jsx` returns ONLY dynamic runtime exceptions.

**Daily Reports tab (~lines 3087-3554):**
This is the most complex remaining section. It includes report list view, report creation form, weather selection, crew assignment, safety notes, and foreman signature.

A. **Report list view:**
- Wrap in AsyncState with emptyIcon={FileText}, emptyMessage={t("Tap + New Report to create the first daily report.")}
- Each report list item: use frm-report-list-item class with FieldCard wrapper. Min-height 44px (FRMN-03).
- Report date: frm-report-date class.
- Report status: use StatusBadge component.
- "New Report" button: `<FieldButton variant="primary" onClick={...}>{t("New Report")}</FieldButton>`

B. **Report creation/edit form:**
- Form wrapper: use frm-report-form class.
- Section titles: use frm-report-section-title class.
- Form rows: use frm-report-row class.
- Labels: use frm-report-label class.
- Values: use frm-report-value class.
- All text/number inputs: use FieldInput with appropriate inputMode.
- All selects: use FieldSelect.

C. **Weather selection:**
- Weather button grid: use frm-report-weather class.
- Each weather button: use frm-report-weather-btn class. Active state: frm-report-weather-btn.active (className toggle, NOT inline background color).
- Min-height 44px on each weather button (FRMN-03).

D. **Crew assignment rows:**
- Use frm-report-crew-row class.

E. **Foreman signature:**
- Use FieldSignaturePad from barrel import: `<FieldSignaturePad onSave={...} label={t("Foreman Signature")} t={t} />` (FRMN-05)
- Wrapper: frm-report-sig class.

F. **Submit/save buttons:**
- Use FieldButton for all actions: "Submit Report", "Save Draft", "Sign Report".

**Site tab (~lines 3638-3701):**
- Section wrappers: use frm-site-section class.
- Photo grid: use frm-site-photo-grid class.
- Photo items: use frm-site-photo class.
- Use FieldButton for add photo / upload actions.
- Empty state if applicable.

**Notes tab (~lines 3702-3953):**
- Notes list: use frm-notes-list class.
- Each note: use frm-notes-item class. Pinned notes add frm-notes-pinned class.
- Note text: use frm-notes-text class.
- Note date: use frm-notes-date class.
- Note input (new note textarea): use frm-notes-input class.
- Toolbar (pin, delete): use frm-notes-toolbar class.
- Pin/unpin buttons: use FieldButton variant="ghost".
- Delete button: use FieldButton variant="danger" with label={t("Delete Note")}.
- Empty state: `<EmptyState icon={MessageSquare} heading={t("No Notes")} message={t("No notes recorded for this project.")} t={t} />`

**Final sweep for ALL remaining hex color literals in the entire file:**
After refactoring these tabs, do a final scan of the ENTIRE ForemanView.jsx for any remaining:
- `#10b981` → replace with frm-phase-active class or var(--phase-active)
- `#3b82f6` → var(--phase-estimating)
- `#8b5cf6` → var(--phase-pre-construction)
- `#6b7280` → var(--phase-completed)
- `#eab308` → var(--phase-warranty)
- `#f59e0b` → var(--phase-in-progress)
- Any other hex colors → replace with appropriate token-based class

FRMN-06 requires: `grep "#10b981\|#3b82f6\|#8b5cf6\|#6b7280\|#eab308" src/tabs/ForemanView.jsx` returns 0. The only hex that may remain is in imported constant references (jsaConstants.js riskColor function) which is out of scope.

**Final sweep for remaining var(--accent)/var(--surface*) references:**
- grep entire file for var(--accent), var(--accent-dim), var(--surface1), var(--surface2) and replace any remaining with var(--amber)/var(--amber-dim)/var(--bg2)/var(--bg3) via CSS classes.

**Dynamic runtime exceptions — the ONLY inline styles allowed after this task:**
Per RESEARCH.md Pattern 7, these may remain as `style={{...}}` with JSX comments:
1. Budget bar fill width: `style={{ width: '${pct}%' }}` `{/* dynamic: budget % computed at runtime */}`
2. Progress bar widths: `style={{ width: '${pct}%' }}` `{/* dynamic: progress % computed at runtime */}`
3. JSA risk badge colors: `style={{ backgroundColor: riskColor(score) }}` `{/* dynamic: risk score color computed at runtime */}`
4. Selected item backgrounds: `style={{ ... }}` `{/* dynamic: selected state */}`

If any inline style needs a class not in Plan 1, add it to styles.js FOREMAN VIEW section.
  </action>
  <verify>
    <automated>bash -c "echo 'Total style={{ count:'; grep -c 'style={{' src/tabs/ForemanView.jsx; echo 'Phase hex check:'; grep -c '#10b981\|#3b82f6\|#8b5cf6\|#6b7280\|#eab308' src/tabs/ForemanView.jsx; echo 'Undefined token check:'; grep -c 'var(--accent)\|var(--surface1)\|var(--surface2)' src/tabs/ForemanView.jsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "style={{" src/tabs/ForemanView.jsx returns less than 15 (only dynamic runtime exceptions)
    - grep "#10b981\|#3b82f6\|#8b5cf6\|#6b7280\|#eab308" src/tabs/ForemanView.jsx returns 0 matches (FRMN-06)
    - grep "var(--accent)" src/tabs/ForemanView.jsx returns 0 matches
    - grep "var(--surface1)\|var(--surface2)" src/tabs/ForemanView.jsx returns 0 matches
    - grep "FieldSignaturePad" src/tabs/ForemanView.jsx returns at least 2 matches (JSA from Plan 3 + reports from this task)
    - grep "frm-report-form\|frm-report-section" src/tabs/ForemanView.jsx returns at least 1 match
    - grep "frm-notes-item" src/tabs/ForemanView.jsx returns at least 1 match
    - grep "frm-site-section\|frm-site-photo" src/tabs/ForemanView.jsx returns at least 1 match
    - Every remaining style={{ has a JSX comment containing "dynamic:"
    - grep "EmptyState" src/tabs/ForemanView.jsx returns at least 7 matches (all tabs with possible empty state)
  </acceptance_criteria>
  <done>ForemanView.jsx fully refactored. Zero static inline styles remain (FRMN-01). Only dynamic runtime exceptions persist with JSX comments. All phase color hex literals eliminated (FRMN-06). Daily reports use FieldSignaturePad from barrel (FRMN-05). All buttons use FieldButton with 44px touch targets (FRMN-03). All tabs show EmptyState when empty.</done>
</task>

</tasks>

<verification>
**Final gate checks (run all after Plan 4 completion):**
1. `grep -c "style={{" src/tabs/ForemanView.jsx` — must be less than 15 (FRMN-01)
2. `grep "emp-tabs\|emp-tab[^-]" src/tabs/ForemanView.jsx` — must return 0 (FRMN-02)
3. `grep "#10b981\|#3b82f6\|#8b5cf6\|#6b7280\|#eab308" src/tabs/ForemanView.jsx` — must return 0 (FRMN-06)
4. `grep "function FieldSignaturePad" src/tabs/ForemanView.jsx` — must return 0 (inline def deleted)
5. `grep "PortalTabBar" src/tabs/ForemanView.jsx` — must return matches
6. `grep "maxPrimary={5}" src/tabs/ForemanView.jsx` — must return 1
7. `grep "var(--accent)" src/tabs/ForemanView.jsx` — must return 0 (undefined token eliminated)
8. `grep "foreman-kpi-value" src/styles.js | grep "text-display"` — must return match (FRMN-04)
</verification>

<success_criteria>
- ForemanView.jsx has zero static inline styles — only dynamic runtime exceptions remain (FRMN-01)
- All phase color hex literals eliminated, replaced with var(--phase-*) tokens (FRMN-06)
- Daily reports use FieldSignaturePad, FieldCard, FieldButton, AsyncState from shared library (FRMN-05)
- All interactive elements meet 44px touch target via FieldButton and frm-* classes (FRMN-03)
- All empty states use EmptyState component with UI-SPEC copywriting
- Zero undefined token references (--accent, --surface1, --surface2) remain
- File compiles and renders without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-foremanview-refactor/05-04-SUMMARY.md`
</output>
